# Pennant for Mac: the SwiftUI rebuild

**Status:** design, 2026-09-25. Milestone N0 is done (2026-09-25: the restore point, the decisions D-055 to D-060 and
the D-052 amendment, the behaviour cases, the ground rules; section 12). Milestone N1 is done (2026-09-25: the sidecar
server, section 5; "As built" below). Milestone N2 is done (2026-09-25: the contract pipeline, section 4.3; "As built"
there). Milestone N3 is under way: its Stage A (2026-09-26: the Xcode project and packages, the server inside the app,
`ServerController`, `AppModel`, the event client, routes, the comparator, backups and the test script; "As built at N3" in
sections 5.2, 5.3, 6, 7 and 8) is built; Stage B, the window shell and first run, is next. Nothing else in this document is
implemented yet. It supersedes the UI parts of the
V2 web plan (`~/.claude/plans/okay-can-we-please-effervescent-cherny.md`, sections 3 and 4). The server-side
parts of that plan (the Front Office contract, the Club Profile, the roster map, the horizon board, the league
wire, snapshots and the GM's desk) carry over unchanged in intent and are scheduled here.

**Owner decisions behind it (2026-09-25):**
- A full native SwiftUI rebuild, done as one big rewrite, with a guaranteed way back to today.
- It should feel like a native Mac app, with Liquid Glass. Deep, not superficial. Elegant to use.
- macOS 26 Tahoe is the minimum. The two users (the owner and his brother) are both on Apple Silicon Macs.
- The owner will join the Apple Developer Program for Developer ID signing and notarization.
- The React UI and Electron retire at cutover, in one final PR that is easy to revert.
- Restore point: tag `pre-swiftui` plus branch `archive/electron-react`, both at today's `main`.
- No postseason odds or buy/sell posture as headlines (they appear only in League Office standings, with their
  basis).
- League places are allowed for Player Value comparisons at a position (an amendment to D-052).
- AI gets slots only; the AI overhaul is a separate project.

Research inputs:
- Apple's HIG, WWDC25 and WWDC26 sessions, and the local Xcode 27.0 / macOS 27.2 SDK interfaces (Swift 6.4).
- The repository as of `87934cf`.

Every Apple API named below was checked in the installed SDK unless marked *unverified*.

---

## 1. The shape of the system

```
┌──────────────────────────── Pennant.app (Swift 6, SwiftUI, macOS 26+) ─────────────────────────────┐
│  Scenes: main window · player windows · club windows · compare · Settings · menu bar extra (opt.)    │
│  Features (local packages) ── PennantDesign (claims, glass, charts) ── PennantKit (models, stores)    │
│  PennantAPI  ← generated by swift-openapi-generator from contract/openapi.json                        │
│  ServerController (actor): spawns, watches, restarts, stops the sidecar                               │
└───────────────┬──────────────────────────────────────────────────────────────────────────────────────┘
                │ HTTP on 127.0.0.1:<random port>, bearer token per launch, SSE for events and chat
┌───────────────▼───────────── Contents/Helpers/node + Contents/Resources/server/*.cjs ─────────────────┐
│  The existing TypeScript server, unchanged in doctrine: import, Player State, Rights, Development,    │
│  Farm and MLB Ops, Player Value, calibration, AI. Plus a new presentation layer that authors every    │
│  sentence the app shows (Claims).                                                                      │
└───────────────┬──────────────────────────────────────────────────────────────────────────────────────┘
                │
   ~/Library/Application Support/ootp-front-office/   (the SAME folder the Electron app uses: D-049 hold)
   league.db · history.db · settings.json · config.json · …   + a data-folder lock + backups/
```

**Why this shape:**
- The server is Pennant's brain: 66,700 lines, with 37,200 lines of tests, every doctrine boundary test and all
  the calibration work. Porting it to Swift would throw away the thing that makes Pennant good. Keeping it also
  keeps D-001 ("the application decides; AI explains") and D-008 ("one domain API") intact.
- Only the part you see is rebuilt, and it becomes thinner than the React UI is today:
  - **the server writes every visible sentence** (section 4);
  - **Swift lays out, formats numbers, sorts, charts and integrates with macOS.**
- The research confirmed the server has no Electron dependency. It runs under plain Node once a small sidecar
  entry exists (section 5).

---

## 2. Design principles: deep, approachable, elegant

These are the tests every screen is held to.

1. **Four questions on the first screen.** Where are we? What are we good at? What are we bad at? What needs me?
   All answered with facts and stated places, never a thin prediction.
2. **The organization is the navigation.** Departments in the sidebar, each with a named head who "prepared"
   its report (staff from the save). Every function lives in exactly one department.
3. **Report → views → decision** in every department, generalizing D-043 and D-046. Detail grows as the GM
   drills down; nothing is hidden, and nothing is dumped.
4. **Depth is one gesture away, never in the way.** Four layers, mapped onto Mac conventions (section 3.3):
   glance, help tag, basis popover, inspector, workspace.
5. **Quiet by default.** One accent (the club's colour), glass only on the controls layer, content opaque and
   dense, numbers first. Colour is never the only signal: an SF Symbol and a word always go with it.
6. **Native all the way down.** Standard components, standard shortcuts, the menu bar, context menus,
   multiple windows, drag and drop, undo, Spotlight, widgets and notifications. Nothing that behaves like a web
   page.
7. **Motion means something.** Numbers roll when they change after an import (`.contentTransition
   (.numericText())`), glass morphs between toolbar states, and the inspector slides in. Nothing decorative.
   Everything respects Reduce Motion.
8. **Honest about uncertainty.** A range is drawn as a band with its most likely value marked. Thin evidence
   is hatched. Unknown is a short sentence, never a zero or a guess (D-018).
9. **Advisory, never a verdict.** Items are "to decide", "to review" or "watching", and link to the owner of the
   question. Nothing is ranked by a hidden score, and nothing writes to OOTP (D-004).
10. **It remembers, and the league is alive.** What changed since the last export, what the GM reviewed or
    deferred, and what the other 29 clubs did.

---

## 3. What the app looks like

A visual mockup (HTML imitation of the SwiftUI look) is at <https://claude.ai/artifact/5DHXS87be3FCP1aHfNhNAs>
(private until shared).

### 3.1 Scenes and windows

| Scene | API | Purpose |
|---|---|---|
| Main window | `WindowGroup(id: "main")` | Sidebar · content · inspector. Multiple main windows allowed (for example, one on Farm and one on MLB Ops) |
| Player window | `WindowGroup(for: PlayerRef.self)` + `openWindow(value:)` | One window per player card; restored at relaunch because the value is `Codable & Hashable` |
| Club window | `WindowGroup(for: ClubRef.self)` | Another club's scouting report (League Office) |
| Compare window | `WindowGroup(for: ComparisonRef.self)` | Two or more players, or us against another club, side by side |
| Staff room | `Window(id: "staff")` | The staff chat (existing AI, unchanged behaviour) as its own window; also reachable as an inspector tab |
| Settings | `Settings { }` | General (save and data folder, import), Appearance, AI providers and keys, Updates |
| Setup | `Window(id: "setup")` | First run: find the OOTP save, import, pick the club |
| Menu bar extra | `MenuBarExtra` (off by default) | Record, next game, desk count, "Open Pennant" |

The last route, the inspector's visibility, table column settings and open player windows are restored per
window (`@SceneStorage`, `TableColumnCustomization` is `Codable`, `.restorationBehavior`).

### 3.2 The main window

- **Sidebar** (`NavigationSplitView`; floats as glass automatically on macOS 26+):
  - The club card at the top: logo, name, record.
  - Departments, each disclosing its views. That's two levels, the HIG maximum for a sidebar.
  - A count badge of items "to decide" per department.
  - A **Following** section: clubs and players the GM follows (drag a player or club here to follow it).
  - Sidebar icons are SF Symbols.
- **Toolbar** (glass groups arranged with `ToolbarSpacer`):
  - Back and Forward, and the view title and subtitle (the date, and how current the data is).
  - Per-view scope controls (for example a level picker or a filter), with `.visibilityPriority` where space is
    short.
  - `.searchable` with tokens (`position:SS`, `club:SF`, `level:AAA`) placed top trailing.
  - An **Ask staff** button, and the inspector toggle.
  - Icons are monochrome. The club tint is used only for the one primary action where there is one.
- **Content:** opaque, grouped sections with concentric corners (`ConcentricRectangle`), dense tables with
  `.monospacedDigit()`, and Swift Charts.
- **Inspector** (`.inspector(isPresented:)`, `.inspectorColumnWidth(min: 280, ideal: 320, max: 440)`). It follows
  the selection, as in Finder and Xcode:
  - the **evidence** behind the selected figure or item;
  - the **player** summary when a player row is selected;
  - the **history** (what changed since earlier exports);
  - the **staff** tab: chat about the selection.

  Controls use `.controlSize(.small)`.

### 3.3 The four layers of depth, done the Mac way

The HIG gives a help tag to a *control* (one line, at most about 75 characters) and puts richer explanation in
a *popover* opened by a click. So Pennant's "hover to see why" becomes this:

| Layer | Gesture | What appears | API |
|---|---|---|---|
| Glance | Read | The headline or figure ("Hitting for power · 28th of 30") | `Text`, `.monospacedDigit()` |
| Hint | Hover | A one-line help tag, written by the server ("Team slugging against the other 29 clubs") | `.help(claim.hint)` |
| Basis | Click the figure or its ⓘ, or press **Space** on a selected row (like Quick Look) | A popover saying: **why** (evidence lines with values); **from** (department, specialist, as-of, sample); **how it's called** (calibrated, provisional or policy line); **not known**; **would change if**; **our philosophy's lean** with the neutral reading beside it. It can be **detached** into a floating panel and kept open while working | `.popover`, `.onKeyPress(.space)` |
| Evidence | **Pin** in the popover, or ⌥⌘I | The inspector shows the full trail: the evidence table, the players involved, thresholds with their stamps, related items, and history | `.inspector` |
| Workspace | "Open in …", double-click, or Return | The owning department's view or decision, or a player window | `openWindow`, `.contextMenu(forSelectionType:primaryAction:)` |

Every one of these is keyboard reachable. VoiceOver reads each claim's text as its label and its basis as custom
content (`accessibilityCustomContent`), so the depth is there without a mouse.

### 3.4 The Morning Report (Front Office)

1. **Masthead** (Apple Sports style). The club's colour band carries the record, division place and games back,
   run differential (with runs scored and allowed), the last five, streak, tonight's game with probable starters,
   and the trade-deadline date.
   - Numerals use SF Pro in a condensed width (`.fontWidth(.condensed)`) and roll on change.
   - It shows objective facts only: no odds, no posture.
2. **This week / since the last export:** chips that open their items (new, resolved, moved, recent results).
3. **How we win and lose.** Each dimension shows its league place on a 30-club strip, split into Strengths,
   Weaknesses and "the rest":
   - scoring runs, preventing runs, on base, power;
   - rotation, bullpen, defensive efficiency, baserunning;
   - recent against season.

   Each line is a claim. Policy lines are top and bottom fifth; below 20 games it reads "too early".
4. **Roster map.** A diamond of the regulars, with the rotation and bullpen beside it. Each position shows:
   - the holder, his Player Value band, and his league place at the position (with the overlap count);
   - the depth behind him, and the farm's next man with his readiness;
   - how long we control him.

   MLB Ops needs are marked on the nodes. It's drawn with Swift Charts and shapes. Clicking a node opens its
   evidence in the inspector.
5. **Your desk.** Items to decide from every department, merged and sorted by stated severity. Each names who
   raised it ("Raised by Jeff Banister, bench coach"). Statuses are Open, Reviewed, Deferred until…, and Handled
   in OOTP, with undo (`UndoManager`).
6. **Department reports.** Compact cards: department, head, summary line, 2–3 key figures and the top items.
   There's an empty staff-memo slot per card for the later AI pass.
7. **Around the league.** The top wire entries since the last export, with followed clubs first.
8. **Game day** (collapsible) and the **GM Briefing** (existing AI, collapsed).

### 3.5 One anatomy for every department report

Every report reads the same way:
- a masthead ("Prepared by {name}, {role}", data as of, a one-sentence summary);
- key figures;
- **To decide** and **Watching**;
- **What changed since the last export**;
- **What we can't see**: the department's unknowns, as short sentences. This makes the fog of war an honest,
  visible feature;
- the staff-memo slot.

The department's views sit beneath it in the sidebar.

| Department (head from the save) | Views |
|---|---|
| **Front Office** (GM) | Morning Report · Storylines (AI) · GM Briefing (AI) |
| **Major League Ops** (bench coach) | Report · Position players · Pitching staff · Bench & coverage · Decision · Lineup · Pitching availability · Schedule & game plans · Depth chart · 40-man & options · Rosters · Season trends |
| **Farm & Development** (minor league staff) | Report · Organization · Affiliates · Assignments · Prospects · Development tracking · Decision |
| **Scouting** (scouting director) | Draft board · Player search |
| **Trades** (assistant GM) | Trade desk (offers, builder, analysis, league fits) |
| **Finance** (front office) | Report · Payroll & budget · Contracts · Free agents · Horizon board |
| **Medical** (head trainer) | Report · Injury report |
| **League Office** | Wire · Club reports · Us vs them · Standings (odds and posture live only here, with their basis) · Leaders · Org comparison · Franchise history |
| **Philosophy & Staff** | Organizational philosophy · Coaching staff |

### 3.6 Signature interactions

- **Drag and drop** (`Transferable` `PlayerRef` and `ClubRef`): drag a player from any table into:
  - the Trade builder;
  - a Compare window;
  - Following;
  - the Staff room ("ask about him").
- **Context menus** on every player and club: Open Card (double-click or Return), Open in New Window, Follow,
  Compare, Add to Trade, Copy Name, Show in Spotlight.
- **Command menus:**
  - **Go**: departments ⌘1–⌘9, Back ⌘[, Forward ⌘], Find Anything ⌘K.
  - **View**: Show Inspector ⌥⌘I, Show Sidebar ⌃⌘S, Basis for Selection (Space).
  - **Club**: Refresh Data ⌘R, Import Export…, Data Status.
  - **Player**: acts on the focused selection via `@FocusedValue`.
- **Search** with tokens and suggestions, across players, clubs, pages and actions.
- **Numbers that roll** when a new import lands, and a banner or notification: "New export read · 3 new on your
  desk".
- **The Trade builder** is a two-sided drop target. Swift Charts draws the value difference as ranges.
- **The Horizon board** is a grid of position × the next three seasons:
  - each cell shows who is controlled and how;
  - prospects sit in a pipeline lane with their readiness range, never placed in a season;
  - a committed-payroll line runs against a budget `RuleMark`.

### 3.7 Visual language

- **Liquid Glass** only on the navigation and controls layer: sidebar, toolbar, inspector, popovers, sheets,
  menus. Standard components get it automatically.
  - Custom glass is limited to a few floating controls (for example the roster map's filter), placed in a
    `GlassEffectContainer` with `glassEffectID` morphing.
  - Never glass on glass, and never glass on content.
  - macOS 27's refreshed glass and the user's transparency setting come for free. Reduce Transparency and
    Increase Contrast are tested on the custom pieces.
- **Colour:**
  - The club accent comes from server-resolved theme tokens: `derivePalette` moves server-side, so
    `npm run check:theme` keeps guarding contrast.
  - Semantic tones (good, bad, caution, neutral, unknown) are asset colours with light, dark and
    increased-contrast variants, kept distinct from rights colours.
- **Type:** SF system text styles; `.monospacedDigit()` in tables; condensed-width SF for scoreboard numerals
  (this replaces Oswald); no web fonts.
- **Icons:** SF Symbols throughout (licensed for Apple platforms), with emoji removed.
- **Shape:** concentric corners (`ConcentricRectangle`, `.containerConcentric`) and capsule glass controls.
- **Empty and unavailable states:** `ContentUnavailableView` with one plain sentence ("Not valued yet: his
  contract terms aren't in the export"). An unavailable department says so and never reads as "all clear".

---

## 4. The presentation contract: the server writes every sentence

Research found about a quarter of the prose the GM reads is authored in React today. This includes:
- label maps in `src/pages/mlb/common.tsx` and `src/pages/farm/common.tsx`, and `platoonCopy.ts`;
- word builders in `valueWords.ts`, `costBand.ts`, `productionConeGeometry.ts`, `dataStatusModel.ts` and
  Payroll's helpers;
- `glossary.ts`, the `stats.ts` catalog, and the Prospects recommendation words;
- Philosophy's `buildIdentity` and Development's "biggest movers".

All of it moves to the server, for three reasons:
1. The Swift app stays a thin, judgment-free renderer (D-001, D-008).
2. The plain-language rule stays enforced **once, in TypeScript**, over every string the app can show.
3. Two clients could never disagree during the transition.

### 4.1 Types (in `server/contract/`)

```ts
type Certainty = 'calibrated' | 'provisional' | 'policy' | 'unknown';         // open enum in the spec
interface Claim {
  text: string;                    // the visible line, GM voice
  hint?: string;                   // ≤ 75 chars, the help tag
  value?: { n: number | null; unit: Unit; low?: number; high?: number; display: string };
  tone: 'good' | 'bad' | 'caution' | 'neutral' | 'unknown';
  place?: { rank: number; of: number; tiedWith: number; overlap?: number };   // league places, stated
  basis: {
    because: Array<{ label: string; value: string }>;
    source: { department: DeptId; specialist: string; asOf: IsoDate | null; gameDate: string | null; sample?: string };
    unknown: string[]; wouldChange: string[];
    lean: { neutral: string; why: string[] } | null;        // explicit, never "absent means something"
    certainty: Certainty; stamp?: string;                    // D-041 stamp shown in the evidence view
  };
  links: Target[];                  // { kind: 'department'|'player'|'club'|'decision', … }
}
interface Row<T> { id: string; cells: Record<string, Cell>; sort: Record<string, number | string | null>; claim?: Claim }
interface Cell { display: string; tone?: Tone; hint?: string; claimRef?: string }
```

- **Tables arrive ready to show:** display strings, raw sort keys (null means unknown, sorted last in both
  directions), and claims where a cell has a basis.
- **Swift ports only presentation:** the unknown-last comparator, number formatting of served values, and chart
  geometry. The comparator's cases live in `contract/fixtures/sort-cases.json` and run in both Vitest and Swift
  Testing.

### 4.2 Endpoints

Everything new lives under `/api/v2/`, so the React app keeps working on the old routes until cutover.

| Endpoint | Purpose |
|---|---|
| `GET /api/v2/front-office/:org` | Masthead, profile, roster map, desk, report cards, wire top, changes |
| `GET /api/v2/departments/:org/:dept` | Full department report |
| `GET /api/v2/views/:org/:dept/:view?…` | One view's payload (tables of `Row`s, sections of `Claim`s) |
| `GET /api/v2/claims/:key` | The evidence trail on demand (for example an MLB need's staff recommendation, which takes about 3.6 s, so it is never in a summary) |
| `GET /api/v2/player/:id` · `/club/:id` · `/compare?…` | Dossier, club report, comparison |
| `GET/PUT /api/v2/desk/:org` · `GET/PUT/DELETE /api/v2/following` | The GM's desk and follows |
| `GET /api/v2/wire/:org?since&club&kind&followed` | Around the league |
| `GET /api/v2/catalog` | Glossary, stat catalog, theme tokens per club, department list and staff heads |
| `GET /api/v2/events` (SSE) | Import progress and finished, job progress, desk changes, freshness. This replaces 8-second polling |
| existing chat SSE, jobs, settings, import, trade analyze | Reused, and described in the spec by the milestone that first uses each: status, import, setup and settings at N2 (for N3), trade analyze at N12, chat SSE and jobs at N13 |

### 4.3 The typed pipeline (no hand-written models)

1. **Source of truth:** the server's TypeScript types. `server/contract/routes.ts` lists every operation (method,
   path, operationId, params, response type, stream).
2. **Build:** `npm run contract:build` runs ts-json-schema-generator over `server/contract/index.ts` and writes
   **`contract/openapi.json`** (OpenAPI 3.1, committed). Two rules apply while building it:
   - Closed string unions are wrapped as *open enums*, so an older app build never fails on a new code.
   - OOTP game dates are a named `GameDate = string` alias with no format (they're unpadded; see AGENTS.md).
3. **Swift:** the `macos/Packages/PennantAPI` package runs Apple's **swift-openapi-generator** as a build plugin
   (types and client, URLSession transport, SSE decoding via `asDecodedServerSentEventsWithJSONData`). Its
   middleware adds the bearer token.
4. **Drift checks in Vitest:**
   - The committed spec equals a fresh build.
   - Every registered route is in the spec and vice versa (reusing `tests/routes.test.ts`'s route walk).
   - Every v2 GET's live response against the synthetic save validates with ajv (`unevaluatedProperties: false`).
   - **Every `text`, `hint` and `display` string in those responses passes the consolidated banned-jargon list**
     (`tests/bannedJargon.ts`, replacing the per-page copies).

   `swift build` of PennantAPI in CI fails on any breaking change.

**As built at N2 (2026-09-25).** `tests/contract.test.ts` and the package's Swift tests hold each point.

| Piece | Where | What it does |
|---|---|---|
| Types | `server/contract/index.ts` | Re-exports the server's own types; a reused route that answered an untyped object now has an exported type beside its handler, which is typed against it (`ServerStatus`, `ImportAccepted`, `Ok`, `ApiError`, `SearchLocations`, `SaveSourceResult`, `SettingsResponse`, `ProvidersResponse`, `Org`, the request bodies). Payloads unchanged. `GameDate` lives beside `parseGameDate` and types the data status's game dates. `Integer` (`server/contract/primitives.ts`, `@asType integer`) types ids and counts, so Swift reads `Int`; a true decimal stays `number`. |
| Operations | `server/contract/routes.ts` | `GET /api/v2/events` (stream), and the reused `GET /api/status`, `POST /api/import`, `GET /api/saves`, `GET /api/search-locations`, `POST /api/resolve-folder`, `POST /api/config`, `GET /api/data-status`, `POST /api/save-source`, `GET /api/settings`, `GET /api/settings/providers`, `GET /api/orgs`. The other legacy routes are not described. |
| Build | `npm run contract:build` (`scripts/lib/contractSpec.ts`) → `contract/openapi.json` | OpenAPI 3.1, deterministic. A string union of two or more values becomes `anyOf: [{type: string, enum}, {type: string}]`; a single literal (an event's `type`) stays a one-value enum. A union of types (`number \| string \| null`, as `Row.sort` will be) becomes one `anyOf` member per type, `"null"` on the last (the generator reads only a `type` array's first type). `X \| null` is written the way swift-openapi-generator reads it: `type: [T, "null"]` inline, or the reference alone with the property left out of `required` (a `{type: "null"}` member makes the generator drop the property). So the published spec is looser than the server there (the strict form the tests use keeps `required`), and Swift cannot tell a missing field from a null one: a future request body (the desk's PUT, N7) must not give "absent" and "null" different meanings. No `additionalProperties: false`. Bearer security. The build refuses two different types with one name, and an exported generic: export a concrete alias instead (`export type ClaimRow = Row<Claim>`), which becomes one named component. |
| Events | `ServerEvent` in the spec; `ServerEventReading` in PennantAPI | Each event is a named component, and `ServerEvent` is their `anyOf` with `UnknownServerEvent` (`{type: string}`) last. The generator makes a struct of optionals, one per shape, and never throws: a known event fills its own and the catch-all; an unknown one, or a known one whose payload did not decode, fills only the catch-all. `event.reading` tells them apart from the generated types: `known`, `unknown(type:)` (ignore it) or `malformed(type:)` (a type this build knows that did not decode: the N3 event client reports it and re-reads `/api/status`, never ignores it). The stream's response is `text/event-stream` with no schema (the generator's pattern); the client decodes with `asDecodedServerSentEventsWithJSONData(of: Components.Schemas.ServerEvent.self)`. |
| Tests | `tests/contract.test.ts`, `tests/bannedJargon.ts`, `tests/apiRoutes.ts`, `tests/contractShapes/` | The committed spec equals a fresh build. Every `/v2` route (a sub-router's mount path included) is in `routes.ts` and every listed operation is registered and in the spec, both ways; a reused route is described only when listed. Every JSON GET answers the synthetic save in its strict shape (ajv 2020, enums closed, `unevaluatedProperties: false`); each POST is checked in the answers safe to cause in a temporary data folder (its 400s; `resolve-folder` and `save-source` also 200), but not the 200s of `config` and `import`, which would start an import. The hello, an import and a job on the live stream validate. The answers and events are captured, made stable, into `contract/fixtures/` (`npm run contract:fixtures` rewrites them; the test fails while they differ), and the Swift tests decode those same bytes. The banned-jargon list applies to every `/v2` `text`, `hint` and `display`; it was tuned on Player Value's pages, so before department copy moves (N8) it needs a scoped exception (per field or department) for plain words it would reject ("Win Pct", "prior season", "waiver priority"), never a weakened pattern. |
| Swift | `macos/Packages/PennantAPI` | swift-openapi-generator 1.13.1 as a build plugin (types and client, public, idiomatic names) over a link to `contract/openapi.json`; runtime 1.12.1, URLSession transport 1.3.1; `BearerTokenMiddleware`, `ServerEventReading`. A second test target, `ContractShapesTests`, generates types from the tests' shape contract (`tests/contractShapes/`) to prove shapes the contract will need (a number-or-string sort key, an `Int`, a concrete alias of a generic). macOS 26, swift-tools-version 6.2. CI builds and tests it on `macos-26` with `--force-resolved-versions`, caching `.build`. The package imports `HTTPTypes` and `OpenAPIRuntime` through the approved packages without declaring swift-http-types. |

---

## 5. The sidecar server

### 5.1 Server changes (small, additive)

- **Entry:** `server/sidecar.ts`, bundled by esbuild into `build/sidecar/server.cjs` beside the two refit
  workers. It has to be `.cjs` and sit in the same folder, because workers are found next to their caller
  (`playerValue.ts:1665`, `saveCalibration.ts:150`).
- **Port:** calls `startServer(0)`, which also fixes the `Number(PORT) || 5178` bug (a random port could not be
  requested). Once listening it prints one line: `PENNANT_READY {"port":N,"pid":…,"version":…}`.
- **Secrets over stdin:** reads one JSON line `{ token, keys: {…} }` from stdin, and exits on stdin EOF or when
  `process.ppid === 1`. macOS has no parent-death signal, and the stdin pipe closes even if the app crashes.
- **Shutdown:** a SIGTERM handler (none exists today) closes the HTTP server, stops the watcher, closes both
  databases and exits 0.
- **Bearer-token middleware** on `/api`, after the existing loopback Host check, with constant-time comparison.
  The server's own self-calls (`chat.ts:99`, `exporter.ts:111`) send the token too.
- **Always bind 127.0.0.1**, passed explicitly because a `.env` in the data folder could otherwise change the
  binding (`config.ts:36-48`).
- **Data-folder lock** `server.lock` (pid and start time). Both the Electron build and the sidecar take it, so
  the two apps can never write the same database at once.
- **Injected keys:** a key source beside `setSecretCrypto`. Keys live in the Mac Keychain and are handed over on
  stdin, never through environment variables (another process run by the same user could read those).

**As built at N1 (2026-09-25).** `tests/sidecarServer.test.ts` (in process) and `tests/sidecarProcess.test.ts` (the
real process under tsx) hold each point.

| Piece | Where | What it does |
|---|---|---|
| Entry | `server/sidecar.ts` | Sets `OOTP_FO_EMBEDDED`, `OOTP_FO_SIDECAR`, `OOTP_FO_BIND=127.0.0.1` and an empty `OOTP_FO_ALLOWED_HOSTS` before any server module loads (the `.env` loader fills only unset variables, so a `.env` cannot widen them). Requires `OOTP_FO_DATA_DIR`; never falls back to a folder inside the app. |
| Handshake | stdin, first line | `{"token":"…","keys":{"anthropic":"…"}}` within 30 s; a token under 32 characters, bad JSON or stdin closing first fails. Later lines `{"keys":{…}}` replace the keys without a restart (the GM changed a key in Settings). |
| Ready and failure lines | stdout | `PENNANT_READY {"port","pid","version"}`, or `PENNANT_FAILED {"reason","message"}` before exiting. Exit codes: 0 stopped cleanly, 1 could not start, 2 no usable handshake, 3 the data folder is in use (`reason: "locked"`, which `ServerController` shows instead of retrying). |
| Stopping | `shutdownServer()` in `server/index.ts` | SIGTERM, SIGINT, stdin closing, or the parent gone (`ppid === 1`, checked every 2 s) stop listening, drop open connections (event streams), stop the watcher, close both databases and release the lock; exit 0, or exit anyway after 4 s. |
| Port | `portFromEnv` in `server/index.ts` | `PORT=0` now means any free port; the sidecar calls `startServer(0)`. |
| Token | `server/apiToken.ts` | Bearer token on `/api`, after the Host check, compared as SHA-256 digests with `timingSafeEqual`. No token set (Electron, `npm run dev`): no check. The assistant's tools and the site export send it (`ownApiHeaders`). |
| Lock | `server/dataLock.ts` | `server.lock` (`pid`, `startedAt`, which app) taken by `startServer`, so by Electron, the sidecar and `npm run dev` alike. A lock whose process has gone, or that cannot be read, is taken over; a running holder is refused by name. Electron shows the refusal instead of retrying another port. |
| Keys | `setInjectedKeys` in `server/settings.ts` | Once set, keys come from the hand-over (an environment variable still wins, as before), key status says `source: "keychain"`, and saving or clearing a key in Settings changes only the in-memory set: the sidecar never writes `credentials.json`. The app stores a newly saved key in the Keychain itself (N13). |
| Events | `server/serverEvents.ts`, `GET /api/v2/events` | Server-sent events: `hello` (the `/api/status` snapshot), `import-started`, `import-progress` (at most every 200 ms, but always on a new file or phase), `import-finished`, `export-pending`, `job`. A 15 s comment keeps the stream alive. Desk changes and freshness beyond the pending export arrive with N7. |
| Bundle | `npm run build:sidecar` → `build/sidecar/` | `server.cjs`, both refit workers beside it, and a `package.json` with the version and the runtime dependencies (not `electron-updater`). Shares its esbuild settings with the Electron build (`scripts/lib/serverBundle.mjs`). |
| Node runtime | `npm run sidecar:node` → `build/node-runtime/` | Node 24.21.0 darwin-arm64, the archive checked against a pinned SHA-256 before extraction and the binary against the pinned version after, renamed `pennant-server`, with Node's licence beside it. |

The bundled `server.cjs` under the pinned binary was run against the test league with a real hand-over: every route
answered, better-sqlite3 loaded, and the calibration refit worker ran from beside the bundle.

### 5.2 Inside the bundle (arm64)

| Path | Contents |
|---|---|
| `Pennant.app/Contents/MacOS/Pennant` | The Swift app |
| `Contents/Helpers/pennant-server` | The official Node 24 LTS binary, pinned by version and SHA-256, renamed so Activity Monitor reads well |
| `Contents/Resources/server/` | `server.cjs`, `value-refit-worker.cjs`, `calibration-refit-worker.cjs`, `package.json`, pruned production `node_modules` (better-sqlite3 built for Node's ABI, so the Electron ABI trap is gone) |

**Signing (inside out):**
1. Each `.node` file.
2. The Node binary, with hardened runtime, a timestamp, and only `allow-jit` and
   `allow-unsigned-executable-memory` (V8 needs these).
3. Sparkle's helpers.
4. The app last: hardened runtime, no JIT entitlements, **no App Sandbox**. A sandboxed Node child would need
   inherited sandboxing and security-scoped bookmarks for arbitrary OOTP save folders and the live `temp/` copy,
   with no benefit under Developer ID.
5. Notarize with `notarytool` and staple both the app and the DMG.

**Privacy prompts.** The child inherits the app's TCC responsibility, so prompts name "Pennant":
- Reading the Mac App Store OOTP container (`~/Library/Containers/…`) triggers "access data from other apps".
- Save folders in Documents, Desktop or iCloud trigger Files & Folders prompts. Info.plist carries the
  `NS…UsageDescription` strings.

A stable Developer ID signature means these prompts and Keychain prompts appear once, not on every update.

**As built at N3, Stage A (2026-09-26).**
- `npm run mac:stage` (`macos/scripts/stage-server.sh`) stages `build/macos-server/` in the bundle's layout: the pinned
  Node binary as `Helpers/pennant-server`, and in `Resources/server/` the sidecar bundle (`server.cjs`, both refit
  workers, `package.json`), Node's licence (`NODE_LICENSE`) and a production `node_modules` (no `electron-updater`;
  every version as `package-lock.json` pins it). The install runs in its own folder (`build/macos-server-install/`) with
  the pinned binary first on `PATH`, so better-sqlite3 is built for that Node and the repository's own `node_modules`
  (whichever ABI it holds) is never touched; the `.forge-meta` trap cannot reach it. The install is reused until the
  lockfile, the runtime dependencies or the Node version change. Pruned: better-sqlite3's sources and build
  intermediates, type declarations, source maps and READMEs. About 150 MB, 116 MB of it Node.
- The Pennant target's last phase, "Embed the server" (`macos/scripts/embed-server.sh`), sets `CFBundleShortVersionString`
  and `CFBundleVersion` from `package.json` (the one place the version lives), copies the stage into the bundle, and
  fails naming `npm ci && npm run mac:stage` when the stage is missing or stale: another version, a server source newer
  than its bundle, or a content stamp (`macos/scripts/stage-stamp.sh`: `build/sidecar/`, `package-lock.json`, the Node
  version) that differs from the stage's. A build that signs
  signs inside out: each `.node`, then the Node binary with the hardened runtime and only `allow-jit` and
  `allow-unsigned-executable-memory` (`macos/Support/pennant-server.entitlements`); Xcode signs the app last.
  `PENNANT_SKIP_SERVER=YES` builds an app without a server (CI); it starts and says the server is missing.
- Development builds sign automatically with the owner's Apple Development identity on team `6T7RV2A4DQ`; no
  provisioning profile is needed, since the app has no restricted entitlement. Secure timestamps and notarization wait
  for N14.

### 5.3 Lifecycle (`ServerController`, a Swift actor)

1. **Spawn** with these variables:
   - `OOTP_FO_DATA_DIR=~/Library/Application Support/ootp-front-office`
   - `OOTP_FO_APP_ROOT=…/Resources/server`
   - `OOTP_FO_APP_VERSION=<CFBundleShortVersionString>`
   - `OOTP_FO_BIND=127.0.0.1`
   - `OOTP_FO_EMBEDDED=1`

   Then write the stdin JSON line, wait up to 30 s for `PENNANT_READY`, and confirm with `GET /api/status`.
2. **Crash:** detected via `terminationHandler`. Restart with backoff (1, 2, 4 … 30 s). After 5 crashes in 2
   minutes, stop and show a `ContentUnavailableView` naming the data folder, with "Show Log" and "Try Again".
3. **Quit:** `applicationShouldTerminate` returns `.terminateLater`, then SIGTERM, a wait of up to 5 s, SIGKILL,
   and then reply.

   **The kill-mid-import check (N1, 2026-09-25).** An import killed partway is safe for the *file* and was not safe
   for the *data*. The importer replaces one table per file, dropping and creating it outside the file's
   transaction; SQLite's write-ahead log rolls back the open transaction, so the database always opens and passes
   `integrity_check`, but it is left as neither export: the files already replaced are the new export's, the one
   being written is incomplete, the rest are the old export's, and `last-import.json` still describes the old one.
   N1 records the interruption instead of changing the importer: `import-in-progress.json` is written as an import
   starts and removed only when it completes (an import that throws partway leaves the same mix, so it keeps the
   marker too), `/api/status` reports `importInterruptedSince`, and the next start imports the export again (without
   the export folder, it reports and waits). A clean stop during an import
   abandons it the same way. `tests/sidecarProcess.test.ts` kills a real sidecar while it writes a 400,000-row table
   and checks all of it. Making the import itself all-or-nothing (one transaction, or staging tables swapped in at
   the end) remains possible later; it was not needed for safety.
4. **Logs:** captured to `~/Library/Logs/Pennant/server.log` (rotated), viewable from Help ▸ Server Log.

**As built at N3, Stage A (2026-09-26).** `ServerController` (PennantKit) holds each step; `ServerControllerTests` drive it
with scripted processes, and `ServerIntegrationTests` with the real staged server on the synthetic league.
- The child gets only `HOME`, `USER`, `LOGNAME`, `TMPDIR`, `LANG`, `LC_ALL`, a fixed `PATH` and the five variables. The
  app's own environment is not passed on, so a provider key exported in a shell cannot outrank the Keychain's. The
  handshake carries a fresh 64-character token and the keys read from the Keychain (`com.dakotawise.pennant.apikeys`,
  one generic password per provider id; saving a key arrives with N13). The read is asynchronous, on a detached task,
  and happens before anything is spawned, so a slow Keychain delays the start but never the handshake. It is meant not
  to prompt (`interactionNotAllowed`), but that governs the data-protection keychain, while the query searches the
  login keychain, where an item whose access list does not trust the build could still show the system's dialog.
- Before anything is spawned, every launch (the first start, Try Again from any state, a crash restart, a start after
  a restore) passes two gates in the controller, so no caller can skip them: a development build must have a chosen
  data folder (section 6), and the folder's first-run backup must be recorded or taken now (section 7.5). A folder
  held by another running server with no backup yet is shown as locked and nothing starts; a backup that fails is
  `.failed(.backupFailed)` and nothing starts.
- Ready means `PENNANT_READY` within 30 s and then `GET /api/status` (three tries, half a second apart). No ready line
  in time, or a status that never answers, is a failure (the process is stopped), not a crash to retry: a hang does
  not cure itself. A server judged unusable, or being stopped, is never published as ready: a late ready line or a
  status answer that arrives while it ends finds no launch phase to be confirmed from.
- Exit code 3 (or `reason: "locked"`) shows the server's own sentence naming the holder, and is not retried; exit code
  2 is a failure; exit code 1 with a `PENNANT_FAILED` sentence is shown at once (`.startFailed`), since waiting does not
  cure its reason. Anything else is a crash: the wait doubles per crash in a row, 1 s to 30 s (a server that ran for a
  whole two minutes starts the count again), and five crashes inside two minutes stop the restarts. Try Again forgets
  them.
- Quit goes only through `QuitCoordinator` (PennantKit). `applicationShouldTerminate` returns `.terminateLater`; the
  server is stopped (SIGTERM, 5 s, SIGKILL) on a detached task and the reply is delivered through the main run loop, so
  neither needs the main dispatch queue, which the nested run loop `.terminateLater` waits in cannot drain while it is
  inside a main-queue job (every main-actor `Task` is one). Asking to quit (`requestQuit()`, used by the SIGTERM
  handler and every future Quit) schedules `NSApp.terminate` on the main run loop for the same reason. A second request
  while a reply is owed is cancelled. When the app is killed outright, the server sees stdin close and stops itself,
  releasing the lock (checked on a real build).
- stdout is read with a readability handler, a line at a time: `FileHandle.bytes.lines` held the ready line back until
  the pipe closed.
- `server.log` rotates at 5 MB and keeps three older files; the token and keys are never written (the handshake is on
  stdin, which is not logged). The failure screen's Show Log opens it; the Help menu item comes with the menus.
- The event client (`EventClient`) passes known events on, ignores a type this build has never heard of (logging it),
  reports a known type that did not decode and re-reads `/api/status` (the model keeps the latest 20 such reports),
  and reconnects while the server is up: each failed or ended connection is logged, and the wait doubles from 1 s to
  at most 10 s while connecting keeps failing, starting again from 1 s after a connection that opened. Cancelling it
  (the server stopped) ends it at once.

---

## 6. Swift architecture

- **Project:**
  - `macos/Pennant.xcodeproj` using buildable (folder-synchronized) groups, so there is almost no project-file
    churn.
  - Swift 6 language mode, default actor isolation `MainActor`, Approachable Concurrency.
  - Deployment target macOS 26.0. APIs new in macOS 27 are used only behind `if #available(macOS 27, *)`.
- **Local Swift packages** (`macos/Packages/`):
  - **PennantAPI:** the generated client and types, plus the token middleware.
  - **PennantKit:**
    - `ServerController`;
    - `AppModel` (`@Observable @MainActor`): server state, current club, freshness, import status, desk counts;
    - per-department stores (`@Observable`), which cache by import stamp and reload with `.task(id:
      importStamp)`;
    - the SSE event client, the Keychain store, routing (`AppRoute: Codable, Hashable`), the unknown-last
      comparator and formatters.
  - **PennantDesign:**
    - `ClaimText`, `ClaimValue` (help tag, popover, pin), `BasisPopover`, `EvidenceView`;
    - `RankStrip`, `RangeBar`, `SeverityBadge`, `Masthead`/`Scoreboard`, `ReportCard`, `ReportSection`;
    - table helpers (`ClaimTable`, column customization persistence), chart styles (range bands, most-likely
      rules, hatched thin evidence);
    - club theming and tone colours;
    - `#Preview`s fed by fixtures.
  - **PennantFeatures**, one target per department: FrontOffice, MajorLeague, Farm, Scouting, Trades, Finance,
    Medical, League, Philosophy, Player, StaffRoom, Setup. Each feature exports a `DepartmentModule`:

    ```swift
    protocol DepartmentModule {
      static var id: DeptID { get }; static var title: LocalizedStringResource { get }
      static var symbol: String { get }; static var order: Int { get }
      static var views: [DepartmentViewDescriptor] { get }       // id, title, symbol, keywords, view builder
      static func badge(from summary: FrontOfficeSummary) -> Int?
    }
    ```

    The app target assembles the registry. From it come the sidebar, the Go menu, the command-palette and
    search results, the Spotlight shortcuts and the default windows. **Adding a function means adding a view
    descriptor; adding a department means adding a module.**
- **App target:** scenes, commands, Sparkle (`SPUStandardUpdaterController`, consent-first, automatic
  updates off), notifications (`UNUserNotificationCenter`), the dock badge (`NSApp.dockTile.badgeLabel`),
  App Intents (`AppEntity` for players and clubs, `IndexedEntity` with `CSSearchableIndex` for Spotlight,
  `AppShortcutsProvider`), and backups.
- **Widget extension:** the record and next game, and the desk (WidgetKit, macOS desktop widgets). Data is
  shared through the App Group **`<TEAMID>.group.com.dakotawise.pennant`**; the Team-ID prefix avoids the
  macOS 15+ prompt. The app writes a small JSON snapshot after each refresh, and the widget never calls the
  server.
- **Rules for Swift code** (in `.claude/rules/macos.md`):
  - No baseball thresholds, no ranking, no copy beyond structural labels (menu and tab names in a String
    Catalog).
  - A number shown is a number served.
  - A script scans the String Catalog with the same banned-jargon list.

**As built at N3, Stage A (2026-09-26).**
- `macos/Pennant.xcodeproj`, written by hand: folder-synchronized groups (`Pennant/`, `PennantUITests/`; `Support/` and
  `scripts/` are shown but belong to no target), one shared scheme `Pennant`, Debug (`com.dakotawise.pennant.dev`,
  "Pennant Dev") and Release (`com.dakotawise.pennant`), arm64 only. The app is Swift 6 with `MainActor` default
  isolation and Approachable Concurrency; the UI-test target is `nonisolated`, XCTest's own isolation. Hardened runtime,
  no App Sandbox. `Support/Info.plist` adds only the three exported drag types to the generated Info.plist.
- Packages: PennantAPI (N2); **PennantKit** (`ServerController`, `SidecarProtocol`, `RestartPolicy`, `ServerLog`, the
  Keychain key source, `PennantClient`, `EventClient`, `AppModel`, `CurrentClub`, `BackupManager`,
  `QuitCoordinator`, the routes, `UnknownLast`, `ServedFormat`); **PennantDesign** (only `ServedColor`, a served hex
  colour, so far). PennantKit keeps Swift's default nonisolated isolation, since it holds an actor and value types used from both sides (`AppModel` is
  `@MainActor` by name); PennantDesign uses `MainActor` like the app. No new dependency: PennantKit names the approved
  runtime and URLSession packages directly.
- `AppModel` holds the server's state, the status (kept current by events), settings, clubs, the current club, the
  data status, the import under way, and `importStamp`: the last import's finish time, which moves only when a new
  import lands. The current club is the server's: `GET /api/settings` serves `organization` (`{id, source}`, from
  `server/viewingOrganization.ts`: the configured organization, else the human-managed one, the rule every server view
  uses), and `CurrentClub` only finds it in `/api/orgs` for its name and colours; a configured club the list no longer
  has stays that club, shown as not found.
- **How stores reload (the plan for every department store):** a store keys its data on `AppModel.storeKey`
  (`.task(id: model.storeKey)`): the import stamp, the current club and the count of backup restores. It is nil until
  the server is ready and its settings are read, so a store loads once at launch; a new import, a club change in
  Settings and a restore each move it. A store keeps its last good data while a reload is under way.
- Routing: `AppRoute` (an open `DeptID` and a view id), `PlayerRef`, `ClubRef` and `ComparisonRef`, all `Codable`,
  `Hashable` and `Transferable` (as `com.dakotawise.pennant.player`, `.club` and `.comparison`).
- The unknown-last comparator orders numbers by value and strings by UTF-16 code units (as JavaScript compares them),
  every number before every string, unknown last in both directions, ties in the order served.
  `contract/fixtures/sort-cases.json` is run by `tests/sortCases.test.ts` (a TypeScript reference) and by
  `UnknownLastTests`.
- `ServedFormat` formats only a served count or share that has no display string (an import's progress).
- A Debug build must be told its data folder (`ServerConfiguration.development`, compiled into Debug only): a scratch
  folder (`PENNANT_DEV_DATA_DIR` or the launch argument `-PennantDevDataFolder`; the log in `PENNANT_DEV_LOG_DIR`, else
  `logs/` inside it), or the real one on purpose (`PENNANT_DEV_USE_REAL_DATA=1` or `-PennantUseRealDataFolder YES`).
  With neither it starts no server and says "No data folder chosen for this development build", naming both. A
  Release build always uses the real folder.
- Tests and `#Preview`s read `contract/fixtures/` where it is, by path; `macos/Fixtures/` was not needed.
- The String Catalog (`Pennant/Localizable.xcstrings`) holds structural labels only; `tests/stringCatalog.test.ts`
  checks every key and translation against `tests/bannedJargon.ts`.

---

## 7. Coexistence and the way back

The rebuild is one big rewrite, but it is built so that *any* point can be abandoned without loss.

1. **Restore point** (the owner approved it on 2026-09-25; created and pushed at N0 the same day):
   - an annotated tag **`pre-swiftui`** on `87934cf`;
   - branch **`archive/electron-react`** at the same commit;
   - both pushed to `origin`.
2. **Where the work happens:**
   - **All work is on `feature/swiftui`**, with milestone PRs stacked into it. `main` isn't touched until the
     owner approves the final merge.
   - `main` keeps taking server-only work, merged into the branch weekly.
3. **The React/Electron app keeps working on the branch** until the cutover PR, which is the *last* PR and
   deletes `src/`, `electron/`, the web tests and the web dependencies. Reverting that one PR restores them.
4. **Data compatibility:**
   - Both apps use the same data folder.
   - Server changes are **additive only**: new tables such as `report_snapshots`, `desk_items`, `following` and
     `standings_snapshots` in `history.db`, and new files. Nothing existing is altered, and the old app ignores
     them.
   - The watchlist's migration into `following` **copies** it and doesn't move it.
   - The data-folder lock stops the two apps running together.
5. **Backup before first run:** the first time the Mac app starts on a data folder, it copies `history.db` (the
   irreplaceable rating history), `settings.json`, `config.json` and `credentials.json` into
   `backups/pre-swiftui-<date>/`.
   - `league.db` (1.28 GB) is re-importable and isn't copied.
   - A Settings button restores the backup.
   - *As built at N3, Stage A (`BackupManager`):* `history.db` is copied with its `-wal` and `-shm`, which with no
     server running make a consistent database. `ServerController` takes it before every launch, so Try Again and
     restarts cannot skip it. A folder whose `server.lock` names a running process (the Electron app) is not backed up
     then, nor recorded, and no server is started on it: once that app quits, Try Again backs up first. A backup that
     fails starts no server, and its partial folder is removed. `backups/pre-swiftui.json` records the backup, so it
     happens once per folder. A restore is all or nothing: it checks that every recorded file is in the backup and
     readable, copies them into a staging folder inside the data folder, then moves the files it replaces (a stale
     `-wal` included) to `backups/before-restore-<time>/` and the copies into place; any failure moves everything back.
     The app stops the server before and starts it after. The Settings button arrives in Stage B.
6. **Identities:**
   - Development builds are `com.dakotawise.pennant.dev` ("Pennant Dev"), so they can sit beside anything.
   - The release app uses `com.dakotawise.pennant`. No Electron installer was ever published, so D-049's hold
     on the app id is not at risk.
   - Keychain items are new (`com.dakotawise.pennant.apikeys`). An optional one-time "Import key from the
     Electron app" button can come later; otherwise the key is re-entered once.
7. **Rolling back:**

   ```bash
   git switch archive/electron-react
   ```
   ```bash
   npm ci && npm run desktop
   ```

   Then, if needed, Settings ▸ Restore backup (or copy `backups/pre-swiftui-*/history.db` back). This procedure
   is recorded in DEVELOPMENT.md at N0.

---

## 8. Verification (how the work is proved at every step)

- **Server:**
  - `npx tsc --noEmit`, `npm test` (including the contract, drift, ajv and jargon tests), `npm run build`,
    `npm run check:theme`.
  - The boundary tests grow to cover `server/presentation/` and `server/contract/`: no rating reads, no
    `players_value`, no AI modules, no `posture` or `playoffs` imports on the landing payload.
  - Plus the import rule keeping `providers`, `ai`, `chat` and `storylines` out of deterministic modules.
- **Swift:** `macos/scripts/test.sh` runs `xcodebuild -scheme Pennant -destination 'platform=macOS' test`:
  - Swift Testing for stores, routing, the comparator (shared cases), formatting and the registry;
  - `ImageRenderer` snapshots of key views from fixtures, written as PNGs for review;
  - XCUITest smoke flows that open each department, a player window and the basis popover, taking screenshots
    as attachments (extracted with `xcrun xcresulttool`, so no screen-recording permission is needed) and
    running **`performAccessibilityAudit`**.
- **Fixtures:** generated by running the server against the **synthetic save** (`tests/syntheticSave.ts`) and
  dumping the v2 responses to `macos/Fixtures/`. No private save data is ever committed. *(As built at N2 and N3: the
  responses are captured into `contract/fixtures/`, which the Swift tests and previews read in place.)*
- *As built at N3, Stage A:* `macos/scripts/test.sh` writes the synthetic league into a scratch folder
  (`npm run synthetic:league`), stages the server, runs every package's Swift tests (PennantKit's include the real-server
  integration test), then `xcodebuild test` on the Pennant scheme with the app on a fresh scratch data folder, and
  extracts the XCUITest screenshots into `build/macos-test/screenshots/`. The XCUITests need UI automation, which the
  Mac's owner enables once (it asks for a password); until then they cannot drive the app. CI builds the app and its UI
  tests unsigned without the server, and runs the package tests.
- **Manual matrix per milestone:**
  - light and dark; Reduce Transparency; Increase Contrast; Reduce Motion; VoiceOver spot check;
  - window widths 900, 1280 and 1728+;
  - two contrasting clubs;
  - the real save, opened through a scratch data folder with `OOTP_FO_DB_READONLY=1` where possible.
- **Release checks:** a notarized build installs and updates through Sparkle on the owner's Mac and his
  brother's.

---

## 9. Milestones

This is one integration branch, and each milestone is a PR into it. Estimates are in working sessions; they are
sizes, not dates.

| # | Milestone | Main contents | Sessions |
|---|---|---|---|
| **N0** | Restore point and ground rules | Tag and archive branch (approved), `feature/swiftui`, this document committed, D-055…D-060 drafts and the D-052 amendment, BEHAVIOR_CASES sections, `.claude/rules/macos.md`, rollback procedure in DEVELOPMENT.md. Owner: Apple Developer enrollment, dependency approvals (below) | 1 |
| **N1** | Sidecar server | `sidecar.ts`, `PORT=0`, ready line, stdin watchdog, SIGTERM, token (including self-calls), bind, lock (also in Electron), injected keys, `/api/v2/events` SSE, `build:sidecar`, pinned Node download with checksum, kill-mid-import safety check | 2 |
| **N2** | Contract pipeline | `server/contract/`, `contract:build`, `openapi.json`, drift, coverage, ajv and jargon tests; the PennantAPI package builds | 2 |
| **N3** | App skeleton | Xcode project and packages, `ServerController`, `AppModel`, window shell (sidebar from the registry, toolbar, inspector, commands, Settings, Setup/import flow), dev signing, test scripts, fixture generation, backups | 3 |
| **N4** | Presentation foundation (server) | `Claim` and `Row`, the org resolver, route extractions (standings, trends, crunch, pitching), severity normalization, Front Office adapters and cache, `/api/v2/catalog` (glossary, stat catalog, theme tokens, staff heads) | 3 |
| **N5** | Design system (Swift) | `ClaimText`, `ClaimValue`, `BasisPopover` (detachable), `EvidenceView`, `RankStrip`, `RangeBar`, `Masthead`, `ReportCard`, table and chart styles, theming, tones, previews, accessibility | 3 |
| **N6** | The Morning Report | Server: `teamSeason`, `clubProfile`, `rosterMap`, `horizon` (fixtures extended; R2/R4/R5 rules from the V2 plan). App: Morning Report, the report template, roster map | 4 |
| **N7** | It remembers; the league is alive | Server: snapshots, desk, following (watchlist copied), `leagueWire`, club reports. App: desk with undo, changes, wire, club windows, Following in the sidebar, drag to follow, notifications, dock badge | 4 |
| **N8** | Major League Ops | Server: MLB copy moved to v2 (label maps, platoon copy, need badges). App: Overview, Position players, Pitching staff, Bench, Decision (problem → why → recommendation → responses → candidates → mechanics) | 4 |
| **N9** | Clubhouse tools | Lineup, pitching availability, depth chart, schedule and game plans, trends (Charts), 40-man and options, rosters | 3 |
| **N10** | Farm & Development | Server: farm copy, Prospects words and Development movers moved. App: Overview, Organization, Affiliates, Assignments, Decision, Prospects, Development tracking | 5 |
| **N11** | Player windows and comparison | Dossier (Overview, Ratings, Value with Swift Charts ranges, Contract & rights, History, notes), Compare window, drag and drop | 3 |
| **N12** | Finance, Trades, Scouting, Medical, League Office, Philosophy | Payroll (Charts plus budget rule), Contracts, Free agents, Horizon; Trade builder (drop targets, range charts, existing AI evaluation); Draft and Search (tokens); Injuries; Standings (odds and posture with basis), Leaders, Org comparison, Franchise; Philosophy editor with the server-side identity endpoint; Coaching staff | 6 |
| **N13** | AI surfaces, native | Staff room (SSE streaming, markdown via `AttributedString`, server-provided player links), Storylines, GM Briefing; keys in the Keychain (decide then between the data-protection keychain, which needs an application-identifier entitlement and so a provisioning profile, and the login keychain, whose per-item access lists can prompt; N3 only reads). Behaviour unchanged | 2 |
| **N14** | macOS integration and release | App Intents and Spotlight, widgets (App Group), menu bar extra (optional), Sparkle with appcast on GitHub Releases (`pennant-v*`), notarized DMG pipeline | 3 |
| **N15** | Acceptance and cutover | Accessibility audit, Instruments pass, parity checklist against the React app (every field, every hover), acceptance by the owner and his brother; then the **cutover PR** (delete `src/`, `electron/`, the web tests and dependencies; docs), and merge to `main` with the owner's approval | 3 |

**Total: about 51 working sessions.** The milestones are strictly ordered. There are two exceptions:
- N1 and N2 can overlap.
- After N7 the department ports (N8–N12) are independent and can be reordered by the owner's priority.

### Dependencies (approved by the owner, 2026-09-25)

Versions and licences were checked on 2026-09-25. Each is added in the milestone that first needs it, at the
then-current release in the same major version.

| Dependency | Checked at | License | Where | Added in |
|---|---|---|---|---|
| swift-openapi-generator, swift-openapi-runtime, swift-openapi-urlsession | 1.13.1, 1.12.1, 1.3.1 | Apache-2.0 (Apple) | Swift | N2 |
| Sparkle 2 | 2.10.0 | MIT | Swift | N14 |
| ts-json-schema-generator, ajv, ajv-formats | 2.9.0, 8.20.0, 3.0.1 | MIT | npm dev dependencies | N2 |
| Node 24 LTS binary (bundled runtime) | 24.21.0 ("Krypton") | MIT | App bundle | N1 |

---

## 10. Decisions to record (drafted at N0)

- **D-055 · Pennant for Mac:**
  - the native SwiftUI client and the Node sidecar;
  - one domain server, and the Swift app renders, never judges;
  - Liquid Glass on the controls layer only;
  - macOS 26 minimum; no App Sandbox; the same data folder as Electron, with a lock and additive-only changes;
  - the restore point and rollback;
  - the Electron and React retirement at cutover.
- **D-056 · The presentation contract:**
  - the server authors every visible sentence (`Claim`, `Row`, `Cell`);
  - `/api/v2`; OpenAPI generated from TypeScript types;
  - drift, ajv and jargon tests;
  - open enums;
  - game dates never typed as dates.
- **D-057 · Club Profile, roster map and horizon:**
  - objective league places; policy lines (top and bottom fifth, 20 games);
  - Player Value positional places with their overlap and basis;
  - no composite score;
  - a pipeline lane, never invented arrival years.
- **D-052 amendment:** positional comparisons may name a league place (owner, 2026-09-25: "OOTP itself shows
  rankings for club positions").
- **D-058 · Pennant remembers:** report and standings snapshots, the GM's desk, and follows (the watchlist
  copied). They record attention, never transactions.
- **D-059 · Around the League:**
  - wire sources and the wording rule (D-020);
  - stated ordering;
  - club reports for any club under the same fog of war.
- **D-060 · The landing page shows no odds, posture or window labels** (owner, 2026-09-25). They live only in
  League Office standings with their basis, until "Playoff odds from the roster" replaces them.

---

## 11. Risks and how they are handled

| Risk | Handling |
|---|---|
| Rewrite fatigue: a long branch with nothing shippable | N6 gives a usable Morning Report on the real save early; every milestone runs; the React app keeps working until the end |
| Moving about a quarter of the UI copy server-side is large | Done per department in the milestone that ports it; jargon tests catch regressions; `Row` and `Cell` keep the moved copy simple |
| SwiftUI `Table` limits (expandable rows, very custom cells) | `OutlineGroup` / `List(children:)` for hierarchies, and a disclosure row pattern; drop to `NSTableView` via `NSViewRepresentable` only if a measured need appears |
| Hover-to-explain isn't Mac-idiomatic | Help tag on hover, popover on click or Space, inspector on pin (section 3.3). Verify with the owner at N5 using a real build |
| Glass legibility and performance | Glass on chrome only; custom glass in containers; test Reduce Transparency and Increase Contrast; Instruments at N15 |
| Signing, notarization and privacy prompts | Developer ID from N3 for dev signing, and full notarization by N14; usage strings in Info.plist; no sandbox |
| Sidecar crash or hang | Supervised restarts, a logged server, and a clear unavailable state; the kill-mid-import check at N1 |
| Contract drift between TypeScript and Swift | Generated client, committed spec, drift test, `swift build` in CI |
| Branch drift against `main` | Weekly merges; server changes additive; `main` takes server-only work |
| Verifying visuals without watching the screen | XCUITest screenshots and `ImageRenderer` PNGs reviewed each milestone; the owner reviews real builds at N5, N6 and N8 |
| An API is new in macOS 27 while the minimum is 26 | Only behind `#available`, with a macOS 26 path that still reads well |

---

## 12. Starting the work

**N0 is done (2026-09-25).**
1. The restore point: the annotated tag `pre-swiftui` and the branch `archive/electron-react` at `87934cf`, both pushed
   to `origin`.
2. `feature/swiftui` holds this document.
3. Drafted: D-055 to D-060 and the D-052 amendment (DECISIONS.md); the presentation cases (BEHAVIOR_CASES.md "Pennant
   for Mac"); the Claude rule `.claude/rules/macos.md` and its AGENTS.md route; the rollback procedure (DEVELOPMENT.md
   "The SwiftUI rebuild"); ROADMAP item 10.
4. Dependencies: all approved by the owner (section 9).
5. Apple Developer Program: enrolled. The owner's Mac has the *Developer ID Application* and *Developer ID Installer*
   certificates on the paid team, and Xcode knows the team, so N3 can sign development builds and use the App Group.
   The five release secrets (DEVELOPMENT.md "macOS signing") are still to be added before N14.

**N1 is done (2026-09-25):** the sidecar server, on `feature/swiftui-n1-sidecar` with its PR into `feature/swiftui`
(section 5.1, "As built"; the kill-mid-import check in section 5.3).

**N2 is done (2026-09-25):** the contract pipeline, on `feature/swiftui-n2-contract` with its PR into `feature/swiftui`
(section 4.3, "As built").

**Next: N3, the app skeleton** (sections 6 and 9). Open a fresh session on `feature/swiftui` (after the N2 PR merges)
and say **"Continue the SwiftUI rebuild at N3 (docs/SWIFTUI_REBUILD.md)."** Branch `feature/swiftui-n3-skeleton` from
`feature/swiftui` and open its PR into `feature/swiftui`. N3 builds on PennantAPI and the operations N2 described; a
route the skeleton needs that is not in `server/contract/routes.ts` is added there first, then `npm run contract:build`.

Read first: AGENTS.md, this document, D-001, D-008, D-018, D-020, D-043, D-046, D-049, D-052 (with its
amendments), D-054 and D-055 to D-060.
