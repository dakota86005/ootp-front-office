# Behavioral cases

The regression protection for baseball behavior across both operations modules and the specialist they share. **159 tests in 14 files** for
MLB Operations, added in its hardening phase ([MLB_OPERATIONS_HARDENING.md](MLB_OPERATIONS_HARDENING.md)),
**371 tests in 18 files** for Minor League Operations, added with it, widened in its hardening phase, again for windowed usage
([MINOR_LEAGUE_OPERATIONS.md](MINOR_LEAGUE_OPERATIONS.md) Parts 2, 7, 8 and 9) and again when the stakes model's hardening found one line drawn three ways, and **96 tests in 6 files** for developmental stakes
([DEVELOPMENTAL_STAKES.md](DEVELOPMENTAL_STAKES.md)). They are not snapshots and they never say
"Player X must be ranked first": each is a baseball invariant, an acceptable-behavior case, or a boundary,
stated so that a failure means either the model has a defect or the invariant was wrong, and either is worth
knowing.

## MLB Operations: how they are organized

| File | What it protects |
|---|---|
| `mlbGoldenHitters.test.ts` (15) | The same bat is a different finding in a different job; a DH is his bat; an unseen glove stays unseen and makes a comparison less firm; speed alone is not a complete offensive player; improving a tool or a glove never lowers an estimate. |
| `mlbGoldenPitching.test.ts` (17) | Roles from usage for extreme profiles (closer, high-leverage, long, low-leverage, not yet clear); the same weak line means different things in different jobs; deployment findings say what / what instead / why; pen-wide findings; the rotation and the pen compete for an arm. |
| `mlbGoldenLineup.test.ts` (5) | One man, one spot; a shared spot is shared, and the partner is named; an unsettled spot is not given a regular. |
| `mlbGoldenBench.test.ts` (14) | Standing at a position is not covering it; an emergency catcher is not a backup catcher; the bench is functions, never a score; a two-position function says which position is thin; what is an attention item and what is a finding. |
| `mlbGoldenPlatoon.test.ts` (12) | Unknown split evidence stays unknown (never "no issue"; an unknown share of plate appearances against left-handers states no cost, never an assumed 0.3; an unknown usual split for his hand is never zero and gives no verdict); thin splits never overwhelm ratings and the league; a rating-supported difference survives a weak sample; symmetry between the hands; the read says what drives it. |
| `mlbGoldenAvailability.test.ts` (9) | An injured player is not an available replacement; a transaction Rights cannot establish is not presented as established; a rehab assignment is not an option; a promotion off the 40-man exposes its prerequisite; a DFA is not an ordinary option; removing rights evidence never rewrites the baseball read. |
| `mlbGoldenPlans.test.ts` (8) | No two plans are the same move; consequences are always stated; a plan that would leave a role below its floor says so; a second move must earn its keep. |
| `mlbGoldenContext.test.ts` (10) | Philosophy and season shade preference and urgency only: every candidate fact identical across five clubs; no candidate added or removed; a blocked option stays blocked; urgency never turns uncertainty into certainty; the neutral stance is recoverable; every lean names its dimension; preference never passes a clearly better option. |
| `mlbExplain.test.ts` (11) | The structured answer to why it was flagged, what moved it, what a neutral club would hear, what context changed and never changes, what would change it, what is unknown; for a strong regular, a moderate case, a pitcher, a depth player, incomplete evidence, a bench gap, an unavailable candidate. |
| `mlbInvariants.test.ts` (12) | Metamorphic relations: a finding belongs to the man and his job, not the group (adding a star or a scrub, or reordering, changes nobody else's); a better player is never a bigger concern; unknown is never firmer than known; aging only gets worse; the role standards are ordered the way the jobs are. |
| `mlbThresholdBoundaries.test.ts` (25) | Every policy threshold at the value and a step either side (since cycle 4 the "too early" line and the "firm read" line (high confidence, a response rather than "keep watching", "more sample would firm it up"), each pinned to the sample it meant before (161.5 and 450 PA for a hitter, 301.5 and 840 BF for a starter, 215.4 and 600 BF for a reliever), and no reader comparing results' trust with a bare number; the "too early" line read against trust in his results as his level and never the blend with his tools, so how much the tools hold results back never decides whether there is enough sample to judge; evidence without the tools weight in force is refused): role floors, regular and partner shares, the platoon minimum and margins, leverage cut-offs, deployment gap, credible-arm line, shift gain and edge, roster floors, IL-return window, the window and season cut-offs. |
| `toolsModelProfiles.test.ts` (13) | The tools model at the corners: monotone, no double counting, no single tool dominates, unlike bats stay unlike, missing tools are never averaged around, running stays small; the line a tool must move to be named is half the league's peers' spread (derived, cycle 4), and with too few peers, or no spread, no profile words are given rather than "no standout". |
| `toolsParamsInForce.test.ts` (6) | The tools model's slopes arrive as the params in force (cycle 4): a different set changes the expectation; no production module outside the model, its reader and its fit names the starting slopes; MLB Operations and the Lineup page read the same reader, and no evidence builder calls the model without the params; the forward-case layer imports no subsystem; the calibration route serves the slopes in force, their source and the tools weight; an affiliate club's Lineup page reads its organization's major league's slopes. |
| `resultsStress.test.ts` (14) | How much current performance should move a belief: a hot week, a slump, a full year, an improving and a declining player, no history, sample size as uncertainty, park effects, baserunning. |
| `toolsBlend.test.ts` (8) | Knowing a player's visible tools never makes his results count more than they would on their own, and the same for a visible glove grade and visible running ratings; the more the tools are trusted, the less the same results move the estimate; the starting tools weight is the results' own K (the tools pull only by their own weight until a save checks its ratings as a forecast); the weight multiplies the K in force, and params without it are refused (cycle 4: the old blend, K × (1 − information), pointed the other way). |
| `mlbPopulation.test.ts` (2) | A peer is a major leaguer: fails without the amateur filter (D-039). |
| `mlbCalibrationFit.test.ts` (19) | The save's own yardsticks (D-053, cycle 1): a floor is never moved by a fit that failed its check; what is checked is what is served (a league far from the starting values fails its check on the lines as served); the aging curve fitted without the starting curve must pass too; ages the league does not reach hold the end values; a league with too few seasons keeps the starting yardsticks and says so; too few clubs, or too early in the season to name the regulars, measures nothing; each lens gets its own line on its own scale; a measurement is repeatable; aging never improves with age and holds the starting curve where the league has few players; a league whose aging changed after the seasons it was fitted on fails its check; the glove weights come from fielding results only and stay the starting values without two seasons of fielding runs, or without a later season to check them on. |
| `mlbCalibrationReview.test.ts` (6) | The starting values are never presented as this league's own ("this league's" only where the save's own fit is in force). The review is judged against the yardsticks in force: results that are ordinary for regulars at a position are not "weak for the role" on the results lens once that lens has its own line (owner decision 2026-09-24), so a "both lenses weak" case becomes "tools lag his results"; a measured standard moves the floor and the finding names the league's own number; the stated decline at an age is the league's own, and a league showing no decline says so, never "lost about 0". |
| `saveCalibrationStore.test.ts` (10) | A fit belongs to its save: a reverted save serves its latest measurement at or before its game date, compared as dates; idempotent per key, never found by another save under the same league id, a failed fit never replaces an adopted one (even forced), never served through a season the league has not completed; with nothing stored every yardstick is a starting value and the page line says so. |
| `yardsticksCopy.test.ts` (29) | The line gives the true reason the starting values serve (no major league, nothing measured yet, too early, games unknown, too few clubs, not enough seasons, a failed check), plainly and with no method words; each yardstick's own reason is in the hover; the hover says in plain words how the league's own yardsticks were checked, including how much a hitter's own split counts and who counts as a long man (and the Pitching Staff page's long-man hover). A starting value the league was checked against says it held up (or did better when checked again), never "not measured", and the review is then served the starting values, never the league's own under another name (cycle 2). |
| `calibrationDetector.test.ts` (16) | A league's own tuning value replaces the starting one only when CLEARLY better on seasons it never saw (owner decision 2026-09-25): a gain that one season's luck carries, or that is too small to matter, is not clearly better however large the sample; a gain the players support but the seasons do not is not clearly better (what a season shares moves all its players together); a point estimate over the minimum whose confidence bound is under it is not enough; a player's seasons are one piece of evidence; no measured spread is no evidence; too few seasons cannot judge at all; better unshrunk and as served, at two refits in a row; once the league's own serve, noise never sends them back, but giving up is easier than adopting: a starting value surely better at all returns. |
| `mlbResultsFit.test.ts` (25) | How much recent seasons count, per league (cycle 2): a league whose true dynamics are the starting values keeps them; a league whose results are clearly noisier and change faster adopts its own once a second refit confirms it, moved the right way; over a save's lifetime of refits, a league whose starting values are within the practical minimum of the best never adopts its own; after a break between imported seasons and the game's own, values adopted on the old regime give way once the starting values are surely better; the confirmation counts consecutive completed-season refits only; a held-out season never takes part in choosing what it judges; a K-only part (baserunning, defense) is judged on K alone, against the same weights; the starting values holding up is a verdict, not a failure; too few seasons keeps the values in force; baserunning and defense wait for the 10 seasons judging needs. |
| `resultsParamsInForce.test.ts` (6) | Every reader of the results lens receives the values in force: the review's evidence and platoon reads change with the fit, the standards are measured under the values about to serve, and no module can read a season weight or stabilization but through them. |
| `runEnvironment.test.ts` (7) | A point of wOBA is worth the runs its own league-season says (a low-scoring league more, a high-scoring one fewer), and a total the export does not record is unknown, never zero: the fixed 1.2 serves only then, and says why; a caught stealing needs only the runs and the outs. |
| `calibrationRefitAfterImport.test.ts` (3) | The refit never starts for a superseded import and never runs on the server's event loop (no worker: skipped, the fits in force stay); a past season whose schedule is not established is left out with its reason, never held to 162 games. |
| `mlbPlatoonFit.test.ts` (8) | How much a hitter's own platoon split counts, per league (cycle 3): a league whose hitters' splits are as individual as the starting value assumes keeps it, and so does one whose gain is under the practical minimum; a league whose hitters' splits are much more individual adopts its own once a second refit confirms it, moved the right way; a held-out season never takes part in choosing what it judges; the league's K applies only where his platoon ratings are not visible (around his ratings the starting value serves); a league whose export has no batting splits by hand keeps the starting value and says so. |
| `mlbToolsFit.test.ts` (11) | What a hitter's tools say, per league (cycle 4): judged only on seasons that came after the ratings were saved (a snapshot taken once a season is under way never stands for it, nor one older than the window; an offseason or spring import stands for the season about to start; each case's gap is recorded); the reason is true in every state (no forward season, too few, enough but thin, one part judged and the other not); the served slopes carry the scale that was checked, so a league whose tools spread results twice as far adopts slopes about twice the size; a save with none decides nothing and says why; a held-out season never takes part in choosing what it judges; a league whose tools work as the starting slopes say keeps them over a lifetime of refits, one whose tools work very differently adopts its own once a second refit confirms it, moved the right way; where results are much noisier than the tools, the tools come to hold them back more, never less than the results alone; the same-season engine check is reported and never decides; nothing is judged before 4 held-out forward seasons, each after one to fit on. The yardsticks hover gives the tools group's true reason in each of those states; the standards are measured under the tools weight and slopes that serve beside them, in force and in the same refit; the league's own tools weight reaches every working estimate, and MLB Operations and the Lineup page read the same slopes (`yardsticksCopy.test.ts`). |
| `mlbBullpenLines.test.ts` (17) | A long man is judged against how this league's relievers are used (cycle 3): the long-man line is the innings per appearance of the league's own longest-working sixth of relievers, served as measured (a league far above 1.6 is served its own) only when the same quantile drawn from half the clubs, steadily, and from the first half of the season, leaves about the same share of the rest; a line that swings with which clubs drew it is not served; it is never below what "multiple innings" means, and a league whose relievers rarely work long says so rather than claiming its longest-working sixth; a measurement that does not hold up keeps the line in force; the hover never claims a season check that did not run; the reliever standards in force were measured under the same line (one line for the tiers and the standards, never two), and a standards measurement from before the line was measured is read as measured under the starting line; the leverage lines are on the league's own scale and a season's wobble within the tolerance moves no reliever's role. |
| `platoonBullpenInForce.test.ts` (7) | Every reader of the platoon weights and the bullpen lines receives the values in force: the review's reliever roles, the pen-wide findings, a reliever's usage notes and the platoon reads all change with the league's own, and no module outside their owners can read a starting value or a default. |

Shared scenario: `tests/mlbGolden.ts` (a club whose fifth starter is far below what a rotation takes, with Triple-A arms who are clear
upgrades on paper, and five organizations that lean every way the layer knows how) on top of `tests/mlbFixtures.ts`. Rights come from the
real evaluator, so the tests exercise the actual three-valued contract.

## Minor League Operations

| File | What it protects |
|---|---|
| `farmGoldenDevelopment.test.ts` (15) | What the level is doing for a player, and what age has to do with it: holding his own is the null reading; a man clearly better than the league has nothing left to learn there; struggling at a level he is YOUNG for is on schedule; a man past the level's age window raises an organizational question, not a developmental one; a claim needs a sample however extreme the rate; nothing to read is not the same as missing evidence; age never lowers the developmental bar and does not change what he has shown. |
| `farmGoldenPlayingTime.test.ts` (31) | One man, one job; competition, not absence, is a conflict; who is squeezed and who is ahead; a crowded job of depth players costs nobody development; unknown stakes stay unknown; no share is read from a club that has not played; a rehab assignee competes for nothing; a prospect who needs starter innings and gets none; a blocked prospect's thin sample IS the finding; a man with no development to cost is not a finding. Hardening: a cover holder (a corner outfielder in centre, a two-way pitcher at first) is named as ahead and counted against nobody's claim; only a REGULAR is a blocker; a designated hitter is batting, not fielding — a quieter finding for a prospect and none for a depth player; an injured man competes for nothing and his review says why; the shares say when a quarter of the job was played by men no longer on the club; two catchers. A designated hitter is no club-level shortage either: his bat IS getting developmental work, so the affiliate does not say it is not. |
| `farmJobSharing.test.ts` (12) | Sharing a job and being short of it are two facts read from ONE line (`shortOfWork` = not used or occasional), the same for a position, a rotation and a bullpen, and the same one the man's review uses (`shortOfWorkVerdict`); the verdict table and the line agree for every level of work. Ten golden cases pairing the club's reading with the man's: an ordinary healthy share; a protected prospect genuinely squeezed (critical at the club, needs attention for him); part-time work that is adequate (a third of the job: no shortage, ordinary) and part-time work that is not (a tenth: squeezed, an opportunity conflict); a recent read too thin to establish a role; a man four games into the club; three men rotating through the outfield and the DH; a DH/field split (worth a look for him, no shortage the club reports); two catchers splitting the job and a third who is not catching; a spot starter sharing the rotation and a starter getting no starts. |
| `farmGoldenCascade.test.ts` (23) | A chain whose every step is defensible, and a stop that is an answer: absorbed, no defensible move, indeterminate, relocates the same shortage, the bottom of the ladder, the step limit. An indeterminate best candidate stops the chain rather than falling through; it terminates however long the chain could be; preference attaches only to a defensible step; nothing is a transaction. Hardening: a pool Player Development has not evaluated leaves the chain indeterminate and says so, and `no_defensible_move` means every candidate was judged; the other defensible replacements are listed as the branch; a dire hole never makes an indefensible step defensible; the pool rule — a rotation hole takes a man taking starts, a relief hole a relief arm, a position anyone who can play it, never a rehab assignee, an injured man or a man two levels down. |
| `farmGoldenAffiliate.test.ts` (16) | Operational health and developmental health are two readings and stay apart: a surplus is never an operational state; a rotation is short when nobody takes the starts, not when the role codes do not add up; a rehab assignee is named, not counted; a prospect who has outgrown the level is separated from a depth player past its window; a player with no line is not assessed rather than judged; every finding carries its evidence with a basis. Hardening: an injured player is not counted as cover and is named at his position; the operational reading on its own is the reading the view carries; the status never says more than the findings do. |
| `farmGoldenRetention.test.ts` (17) | Three questions, three owners: the developmental outlook is identical at every philosophy; pressure is what makes a closed runway a question; the stance is a lean that is always shown and can never turn a retain into a question; a 40-man player, a major-league contract or an injured list is not the farm's decision and the process that owns it is named; the farm's own reading is reported beside the guardrail. Hardening: an open runway is a development case whatever this season's line says; a closing or closed runway with nothing to read is indeterminate, not negative; the same at every philosophy. |
| `farmInvariants.test.ts` (14) | Metamorphic relations: a finding belongs to the man and his job, not the group (an unrelated player joining, or a reordering, changes nobody else's); a better reading is never a bigger problem; more developmental time never makes him a concern; the window only narrows with age; unknown is never firmer than known; congestion counts the path, depth counts what he can play; a shape is not read off a season that has not been played. |
| `farmThresholdBoundaries.test.ts` (13) | Every policy threshold at the value and a step either side: the level-standing percentiles, the two sample lines, the age-relative-to-level lines, the positional, rotation and relief capacities, the club-games gate, and the declared structural floors. |
| `farmOperationsBoundary.test.ts` (75) | The architecture, now over fourteen modules including the contract, the routes and the pure recent-usage module: no prohibited value field, no rating column, no `gloves()`; the pure modules open no table; nothing writes; no farm module holds a development threshold; philosophy reaches only the retention input, the service and the contract, and never a pure judgment; the developmental outlook is computed before philosophy is read; roster state comes from Player State; every threshold is declared once; the farm is not imported upstream; MLB Operations asks for the consequence and rebuilds no cascade; every conclusion keeps an indeterminate state. |
| `farmRecentUsage.test.ts` (26) | The recent read on its own (D-048). The window is the club's last GAMES, not days, and the whole season for a club that has played fewer; an export with no game log is UNAVAILABLE — null, not thin. When he joined, by source: OOTP's dated move first, the game log's bound without it, the LATER of the two when both speak, established when the readable log shows no move, and UNKNOWN — capped at thin — when there is no log and he was never seen before the window. Evidence is sufficient from the minimum, thin one game below, none for a man who joined after the last game; a game missed to a recorded injury is not counted; fewer observable games never raise the evidence. Fielding starts, DH starts, bench appearances and the lineup are separate; a line from an earlier stint is history; which job a pitcher is competing for is current state, then the window, then the season. |
| `farmGameLogAdapter.test.ts` (12) | The adapter against real SQL, pinning two traps found on the real import: OOTP writes dates unpadded, so `2030-5-9` sorts after `2030-5-10` as text and a window cut by string order is the wrong fifteen games; and doubleheaders exist, so a date is not an order. A scheduled game that was not played is in no window; a line belongs to the club he played for that day; one starter per position per game; a club with no games is unavailable rather than idle; the last game elsewhere bounds an arrival; only a non-day-to-day injury of this season is an absence; OOTP's next five starters are read as current state. |
| `farmGoldenWindowedUsage.test.ts` (40) | The fifteen temporal cases. A former regular promoted away no longer blocks (and the season alone still says he does — the stale reading); a former starter no longer occupies a rotation spot; a newly promoted player with a tiny destination sample is not bench depth, and his review opens with when he arrived; a recent arrival stays uncertain at five games and is read at six; an injured former regular is history, not cover; a rehab assignee's starts are set aside and he is never material (D-026); the season leader who stopped playing does not block, and the man who took over is recognised on an eighth of the season's innings; a move from relief to starting, and from starting to relief, is visible and stated as a fact; an affiliate transfer resets the current competition without erasing the record; an unrelated move changes nothing here; philosophy cannot reach usage; Player Development's answers are identical with and without the window; a converted reliever is not rotation cover in a cascade. |
| `farmWindowedInvariants.test.ts` (36) | Metamorphic relations of the window. A departed man is never ahead of anybody or anybody's blocker at any share of the window, and raising what he did before he left cannot restore him — while more of it is named as history, never dropped. More recent work at the job never lowers his level or share, and the regular line in starts is the season's line in innings. Below the minimum his role is unknown whether he played every game or none, a thin read never makes him squeezed or a blocker, and from the minimum on the same usage is read for what it is. One level apart is not a disagreement; two is, and is said. A lone claimant who is not playing is read as not playing. Without a game log nothing changes. Hitter patterns: the everyday shortstop, the utility man who claims ONE job, two catchers splitting, the designated hitter, the outfielder moved to centre, the man back from an injury. Pitchers: two of three turns is a rotation member; a starter four games in who is in OOTP's next five holds a spot on current state; one OOTP has starting who has relieved all year is a role change under way, not a blocked starter; six men for five spots names the one not starting LATELY. Relief: an arm is measured over the games he could have pitched in, and the window confirms or clears a shortage but never raises one. Retention takes no usage input. MLB Operations receives the farm's own answer, `currentOpportunity` included, untouched. |
| `farmMlbIntegration.test.ts` (13) | The contract end to end: the answer carries the vacated job, the chain and where it stops; MLB Operations reports it rather than rebuilding it; a player joining carries no farm answer; a man not on an affiliate is said to have none; an unresolved hole is never phrased as an illegality; the workspace serves the whole organization and accounts for every player it could not assess. Hardening: the answer is identical with and without a shared session; MLB Operations is told the same operational status the workspace shows, before and after; an arrival is answered with the job taken up and who holds it, never a cascade, on its own route. |
| `farmGoldenArchetypes.test.ts` (14) | The archetypes through the composed review: a young player over a tiny sample is indeterminate, never a demotion case, and on schedule once the sample supports a claim; older depth dominating a lower level is an organizational question however defensible the move up; a high-upside prospect with incomplete evidence has his results read and his stakes left unknown; a mature low-upside bat is a promotion case only when Player Development calls the move defensible, and quietly; old for the level is closing, then organizational; strong tools never promote poor results; strong results never finish the argument alone; an older player never gets a lower bar; each kind of missing evidence stays missing. |
| `farmPeerPools.test.ts` (3) | The two contaminations measured on the real import, pinned against the fixture: the same line read against two leagues' own environments gives two percentiles that follow the league (F-1-farm); a league too thin to compare says so; signings nobody has assigned never move the level's rostered average age (F-0-farm). |
| `farmDevelopmentIndeterminate.test.ts` (8) | An indeterminate Player Development judgment travelling into the farm: reviewed as indeterminate, never a preferred alternative, no numeric protection anywhere, a cascade that stops rather than following him, and the same judgments and verdicts in both payloads under every philosophy. The same developmental stakes for every player at organizations that lean opposite ways — in the farm's payload, Player Development's, MLB Operations' candidates and the retention outlook — and whether the organization's hitters are hot, cold or have no line at all. |

Shared synthetic evidence: `tests/farmGolden.ts`. Protection tiers come from the real evaluator and abilities
from the adapter's own `syntheticScoutedAbility`, so the cases exercise the real branded types.

## Developmental stakes (Player Development)

Added with the stakes model (D-050, [DEVELOPMENTAL_STAKES.md](DEVELOPMENTAL_STAKES.md)). The tier is not a prospect
list, so no case says who ranks first or names a player.

| File | What it protects |
|---|---|
| `developmentalStakesGolden.test.ts` (30) | The eighteen cases the phase was given, as invariants. Youth is not talent: an 18-year-old in the Dominican league with ordinary tools is not a priority however wide his gap, and one with strong visible upside is protected. The same ratings at 18 and at 27 read differently while the CEILING reads the same; a 24-year-old repeating a level with modest tools gets no inflation; a 27-year-old Triple-A depth player with nothing left to project is organizational depth and his ceiling is still named; an older player with genuine visible upside is not depth because of his age alone; the same profile on schedule in full-season ball and 2.7 years behind the Dominican league's is not the same thing. A peer group is context: moving between affiliates does not re-tier him, a weak cohort cannot manufacture a core prospect and a strong one cannot erase one. Missing potential, current or age leaves the stakes unknown; a missing level context or a league too thin for an age profile is said and discounts nothing. Philosophy, production and usage have nowhere to enter. Downstream: a contemplated major-league role reads his stakes and cannot change them, and re-tiering never makes a prospect easier to use as cover than veteran depth; a playing-time conflict reacts to the tier while every man's work level is unchanged; a cascade step's defensibility never reads the tier; a decision another process owns stays that process's at every tier and philosophy. The explanation says the ceiling, then what is left, then how the two made the tier ("that ceiling alone would set protected prospect; it is lowered two steps"), never a score, never a rank or a hidden number, and why it differs from the composite it replaced. |
| `developmentalStakesInvariants.test.ts` (15) | Metamorphic relations swept over every age × current × potential × schedule × kind a player can occupy: a higher visible potential never lowers his stakes; a year older never raises them; further behind his level's schedule never raises them and AHEAD of it never raises them either; nobody is ever above what his ceiling allows with everything ahead of him; realizing a projection never raises them; removing a rating makes the tier unknown and never lower; removing level context never lowers it; the tier is a function of his own evidence (two players with the same evidence, the size or strength of his league); a one-level move for a man behind neither schedule changes nothing; the consumers' semantics are ordered the way the tiers are; MLB Operations reads the tier the farm reads, for every contemplated role. Every line a hair either side; the whole lookup table. |
| `developmentalStakesBoundary.test.ts` (12) | Static: only the context reader calls the evaluator, the major-league context module is handed a protection and cannot compute one, every module that serves a tier obtains it from a reader with the age as the export has it (never `Number(age)`), and the farm reads "how old is he for his league" from that reader alone; nothing decides anything with the superseded composite, which is read only after the tier is assigned; the evaluator opens no table and imports no philosophy, result, usage, history or farm module; the reader reads objective facts only, requires a roster row and groups by league; every stakes constant is declared once and the age-for-level lines are read, not redeclared; no score is served anywhere; nothing names a prospect rank; Player Development's authorization still does not read the tier. |
| `developmentalContext.test.ts` (14) | The one peer frame, against real SQL, for the contaminations earlier phases measured: the same 20-year-old is behind one league's schedule and not another's at the same level code (F-1-farm); signings nobody has assigned do not move a league's age profile (D-039, F-0-farm), nor does a retired man; a league under the minimum uses the level's pool and says so; with no pool the schedule is `not_established`, nothing is discounted and it is said; a club the export does not describe gives no context; another player joining the league leaves this one's ceiling alone; a reader is one request's view and nothing is cached past it. An age the export does not state (null, 0, negative, NaN) leaves the tier indeterminate and discounts nothing, even where a null would once have arrived as a zero. |
| `developmentProtection.test.ts` (16) | The contract every consumer relies on: every tier reachable and in order, the null discipline (`hasKnownTier`, `requireKnownProtection`), manual protection, no score. |
| `developmentalStakesHardening.test.ts` (20) | The boundaries, attacked from both sides over the whole grid: one birthday moves the tier at most one step and never up, for every potential, gap, kind and league age from 16 to 35, even when it also crosses a schedule line; the band edges are the declared ages; weak and strong young players are told apart by the ceiling, never the age; an older late developer keeps a tier while his ceiling is visible and unrealized. One potential point moves the tier at most one step anywhere on the 20-80 scale; the pitcher lines are lower; a large gap and none differ by at most the projection step; a missing rating is unknown at every point. The schedule line at exactly 1.49 / 1.50 / 2.99 / 3.00, through floating-point averages of any roster size, never from a NaN; one unrelated player moving a league's average moves nobody more than one step; the schedule takes at most two steps and only from a man whose age still has them; a level pool is named and an unavailable one discounts nothing. Unknown in every consumer: never squeezed and never "no stakes" in playing time; no relief and no established route in MLB Operations; indeterminate, never exhausted, in retention — and a depth tier is not a release. |
| `stakesLines.test.ts` (23) | The ceiling lines are the league's own (cycle 4): measured from its active major leaguers at each import and served as measured (actual composite values, no pull toward the starting lines) once a tie-aware club split holds steadily; a league far from the starting lines is served its own, not failed for being different; many players exactly on a line do not fail the check; a small or fictional league keeps Pennant's starting lines and says why; a measurement that swings with which clubs drew it is not served, and one that does not hold up keeps the league's own lines in force; the starting lines are named as Pennant's and the league's own as his organization's major league's (never "this league's", which a minor leaguer would read as his own league); "the lines moved, not anything about him" is said only on the export the move was measured on, only when his ratings, age and level are what they were at the import before, and only when his tier then (under the old lines) differs from now; a player whose own potential moved too (the regular line 50 to 49 while his potential fell 51 to 49) gets no such sentence, and a band move that changes no tier says nothing; a league too thin to measure keeps its own earlier lines and says it was not measured, never that it failed; the stakes stamps on the farm's thresholds table carry no cycle, decision number or file name; the evaluator refuses to tier without the lines in force; a reverted save serves the measurement at or before its date; an affiliate reads its organization's major league's lines; the lines read no result, usage, philosophy, Player Value or MLB Operations answer. |
| `farmArrivalHolders.test.ts` (4) | Who holds the job an arriving man would take up is a fact about the club: named for a veteran with no stakes of his own, identical for a protected prospect, and the arriving man is never among the displaced (B-1; fails on `main`). A departure names the man left sharing his job whether or not the club contested it (H-3). |

`farmDevelopmentIndeterminate.test.ts` gained the end-to-end philosophy case: every player's tier, reasons and
readings are identical, in the farm's payload and in Player Development's, at organizations that lean opposite ways
on every dimension the farm reads.

## Player Value

Written in phase 0, before any code (D-052, [PLAYER_VALUE.md](PLAYER_VALUE.md)). Each invariant below becomes an
`it` in the named file in the phase that builds its concern, and must fail first for the reason expected. Value
describes and authorizes nothing, so no case says who is worth more or names a player. **Phase 1 built
`playerValueControl.test.ts` (11), the phase-1 half of `playerValueCost.test.ts` (7, with the phase-2 bands as
`it.todo`), `playerValueBoundary.test.ts` (35) and, after the owner's Super Two ruling, `playerValueSuperTwo.test.ts`
(6)**, from synthetic evidence in `tests/playerValueFixtures.ts`. **Phase 2 built `playerValueFinances.test.ts`**
(the club's value of a win stays `it.todo` for phase 5) and widened the boundary test. **Phase 3a built
`playerValueProduction.test.ts` (53) and `playerValueProductionFit.test.ts` (6)** and widened the boundary test again;
the 3a cases were written first and failed for the expected reason (no `projectProduction`, no `production` on a
valuation). The cases added with the owner's mid-phase decision (D-053: the per-save fit, the gate, the refit, the
prior, the 50% band, injury proneness) arrived after the code, so each was checked instead by breaking the code it
guards and watching it fail. **Phase 3b built `playerValueRatings.test.ts` (19)** and widened
`playerValueProduction.test.ts` (54), `playerValueProductionFit.test.ts` (7) and `playerValueBoundary.test.ts` (111).
Every 3b case was run against the pre-3b code (the phase 3a commit) and failed there for the expected reason (no
ratings path: `ratingsEvidence is not a function`, no ratings fit, no minor-league usage reader; the boundary cases found
no ratings modules and no ratings refit); the ratings module was drafted alongside them, so that check, not the order of
writing, is the fail-first evidence. The supervisor's playing-time case ("a better player keeps more of his playing
time") was written before its code and failed on the 3a code for the expected reason (both players kept identical
usage, 338.5 plate appearances each). **The hardening (2026-09-23)** added six cases to `playerValueFinances.test.ts` and
built `playerValueCrossSave.test.ts` on `tests/syntheticSave.ts`; each real hardening case was run against the
pre-hardening modules and failed there for the expected reason (a 60-game prior season read basis A at 2.7 times the
price, a thin pace and a thin market still priced, club money shown in dollars without financials, an orphaned league's
season blank). The boundary test's hardening was checked by mutation instead: each check failed on a deliberate breach the
earlier test passed.
`payrollControl.test.ts` keeps its earlier cases, now driven through the timeline. **Hardening F2 (2026-09-23)**
added the four "hardening F2" rows at the end of the table (contracts, control, Player Rights and the consumers). Each
case was written before its fix and failed on the phase 3b code for the expected reason (an option still open in the
season under way, no `opt_out` status, a blank row read as `minor_league`, an injured-list low edge of nothing, the
schedule ignored, trips counted by class, no margin constant, `signed` for an option, dead money silently $0, decimal
service, a $0 budget, a dropped option contract); two older expectations moved with the doctrine (a trip already taken
is no longer counted again, and service reads as years.days).
**Hardening F1 (2026-09-23)** added the cases in its row below (production, fit, fit store and cone) before the fixes; all 42 were run against the
phase 3b code (f1e0911) and failed there for the reason expected (a band above the ceiling, the held-out bias growing
to +0.19 wins on a league with selection, no `judgeGate`, a forced failing refit replacing the adopted fit, a ratings
cone saying "Calibrated on this save", and so on).
**Hardening F4 (2026-09-23)** added the "hardening F4" row of the table (prospects: the arrival chance, playing
time by quality, the arrival gate, two top-level leagues, partial ratings in the blend). Each case was written before its
fix and failed on the integration branch (6c615f5) for the reason expected; the output is kept with the hardening notes.
**Hardening F5 (2026-09-23)** added the "hardening F5" row (the arrival model judged as it is served: rolling origins,
recency and two-way clustered errors, the owner's option C applied to arrivals; a snapshot read at its own point of the
season; an unknown's basis names what it rested on). Each case was written before its fix and failed on the integration
branch (a32245f) for the reason expected; the output is kept with the hardening notes.
**Hardening F6 (2026-09-23)** added the "hardening F6" row (the arrival model adopted horizon by horizon, the owner's
option (b): a contiguous run of passing horizons is served, a later season is not established with the gate's finding, a
total over it is not a number, and the cone and every label say so). Each case was written before its fix and failed on
the integration branch (56a563e) for the reason expected; the output is kept with the hardening notes.
**Phase 4a (2026-09-23)** added the "phase 4a" row (the cost of controlled seasons: the renewal spread and the
arbitration ladder measured per import, ranges and open seasons covered, the prior only where the regime is MLB's,
unknown where evidence is missing, projected cost never committed). Each case was written before its code and failed on
`origin/main` (868d441) for the reason expected (no `playerValueCost` module, no `arbitrationRegimeOf` or
`tripIfEligible` in Player Rights, no `costs` on a league's finances, no projected money on Payroll, no `nextCost` on
Contracts, no cost on the cone); the cone's rendering case was checked against the unchanged component the same way.
**Phase 4a review (2026-09-23)** added the "phase 4a review" row and reworded two cases of the "phase 4a" row (an
arbitration season is never *assumed* to be the minimum; a contract at the minimum is kept out of the line without naming
a transaction). Each case was written before its fix and failed on `feature/player-value-phase-4a` (6020184) for the
reason expected; the output is kept with the review notes.

**Phase 4b (2026-09-23)** added the "phase 4b" row (the measured price of a win across imports: the per-import contract
snapshot, observed changes read through Player Rights at the earlier import and never given a transaction type the
export does not carry, the ratio-of-sums price with its resampled band, adoption only when narrower than the opening band
with its sampling, observed awards and reserve-clause renewals, replacement from freely available talent, the price
history on Payroll). Each case was written before its code and failed on the phase 4a commit (6020184) for the reason
expected: `playerValueSignings.test.ts` and the cross-save sequences could not load (no `playerValueSignings` or
`playerValueContractStore` module), `priceHistory is not a function`, Payroll's price line listed no history, and the
boundary test found neither new module, no third writer and no `SIGNINGS_POLICY_CALIBRATION` stamp.
**Phase 4b review (2026-09-24)** added the "phase 4b review" row (a winter read by the calendar, an extension and a deadline acquisition never a market price, two timelines never compared, readings named for what the export shows, the measured price read as bases like the opening with its unit named and adopted only on the realized reading and the class's range, replacement never changing the unit, awards scored with their sharpness, a capture that reads only the import before it). Each case was written before its fix and failed on `feature/player-value-phase-4b` (cf27c0f) for the reason expected; the output is kept with the review notes.

**Phase 4 owner decisions (2026-09-24)** added the "phase 4 owner decisions" row (the price in force per win produced,
Payroll's players combined as independent, an arbitration salary never below the previous season's, the contract history
kept at the winters). Each case was written before its code and failed on `feature/player-value-phase-4b` (e077265) for
the reason expected; the output is kept with the decision notes.

**Phase 5a (2026-09-24)** built the two phase-0 rows for surplus and the invariants, refined as the "phase 5a" row below
(neutral contract surplus and the retention margin, season by season, with every component; the sunk-cost rule; the owner's
5% discount; one level of replacement on both sides; one valuation whichever read asks). Each case was written before its
code and failed on `origin/main` (89e2aad) for the reason expected; the output is kept with the phase notes.

**Phase 6a (2026-09-24)** added the "phase 6a" row (the player card's header and Contracts migrated onto Player Value:
no hidden value figure on the card, the header saying what the Value section says, Contracts showing no verdict and
every figure as Player Value serves it, unknowns kept unknown and sorted last, the GM told how current the data is, and
the page in plain words). Each case was written before its code and failed on `origin/main` (a7f4aef) for the reason
expected (no `header`, `scouted`, `path`, `value`, `ourView` or `freshness` on the payloads, `recommendation` and the
percentiles still sent, no `freshnessCue`, no `HeaderValue` or `ContractsView`, the boundary still finding
`players_value` readers and the card's contract query in `player.ts` and `contracts.ts`, the dashboard still counting
extension candidates); the output is kept with the phase notes.

**Phase 6b (2026-09-24)** added the "phase 6b" row (the Trade Center, the trading block and the AI's trade context read
Player Value: both sides' decompositions side by side and the difference between them as a band with its components,
owner Q-8). Each case was written before its code and failed on `origin/main` (a7f4aef) for the reason expected; the
output is kept with the phase notes.

**Phase 6c (2026-09-24)** added the "phase 6c" row (Free Agents and the AI's value context read Player Value; Payroll,
Free Agents and the Trade Center say how current the export is). Each case was written before its code and failed on
`origin/main` (51bbbc8) for the reason expected; the output is kept with the phase notes.

**Phase 6d (2026-09-24)** added the "phase 6d" row (the Roster's scouting column, the Lineup and Org Comparison taken off
`players_value`: the Roster shows the organization's scouted view, the lineup reads every rating through the evidence adapter
with its choices unchanged where the evidence is the same, and Org Comparison is rebuilt on Player Value and objective facts
with no hidden score). Each case was written before its code and failed on `origin/main` (51bbbc8) for the reason expected;
the output is kept with the phase notes.

**Phase 6e (2026-09-24)** added the "phase 6e" row (the migration's cleanup: no module reads `players_value` and the boundary
can only stay empty; Payroll in plain words with its basis in the hovers; the trading block read on the export's freshness;
the players who might reach the market listed on Free Agents; the briefing's "Worth a look this week"). Each case was written
before its code and failed on `origin/main` (38cd361) for the reason expected; the output is kept with the phase notes.

| Planned file (phase) | Invariants |
|---|---|
| `playerValueControl.test.ts` (1) | A missing free-agency or arbitration rule leaves the season's control `indeterminate`, never six years or three. A missing service time is unknown, never pre-arbitration. A minor leaguer's control is read from his parent league's rules, never from his own league's zeros. A player whose projected service straddles a threshold has that season `indeterminate`, with the dates on both sides stated. Arbitration and free-agency eligibility come from Player Rights and are never re-derived from service time in a value module. In a league whose contract regime is not MLB's, the year before the arbitration line stays `indeterminate`, never eligible and never ineligible (the Super Two cases are in `playerValueSuperTwo.test.ts`). `has_received_arbitration = 0` is not evidence that he has not been through arbitration. |
| `playerValueSuperTwo.test.ts` (1, owner ruling 2026-09-22) | OOTP applies Super Two under MLB rules (owner-attested): a player with two to three years of service is arbitration-eligible when he ranks in the top 22% by service of the league's two-to-three-year class that banked at least 86 days in the season just ending. The cutoff is computed from the export's own class, once per class, as a range, because every member's service is projected to season end. Above the cutoff's high edge, with the 86 days, he is arbitration-eligible, and the reason names his service, the cutoff, the rule and the owner's attestation. Below its low edge he is pre-arbitration. A projected service overlapping the cutoff's range is `indeterminate`, and the reason names both edges of both. Short of 86 days in the season just ending he is pre-arbitration whatever his rank. A league whose contract regime is not MLB's (free agency 6, arbitration 3, a 172-day service year) keeps the window `indeterminate`. A missing input (a class member's service, this season's days, a rule) leaves the window `indeterminate`, never eligible and never ineligible. |
| `playerValueCost.test.ts` (1–4) | An arbitration-eligible player's cost path is a band, never the league minimum and never a point. A pre-arbitration renewal is a band that starts at the minimum. An option is shown on both branches. A no-trade clause, buyout or minor-league salary the export does not populate is unknown, never none and never $0. |
| `playerValueFinances.test.ts` (2; the club's value of a win in 5b) | A league without financials yields value in wins, with dollars `unknown` and the reason stated; so does a league whose market contracts carry no salary, and a reserve-clause league, where no contract is a market price. The opening price of a win always carries its basis, its band and a floor below it, every basis names its population, salary, wins and assumptions, and it is labelled the imported market. A single reading is not a band: the price is `unknown` rather than a point. The price only narrows as observed signings accumulate, and never on one import alone: re-importing the same save, or holding any number of snapshots of it, leaves the price exactly as it was. Which contracts are market prices is Player Rights' free-agency answer for this season: an `indeterminate` answer is neither market nor below it, whatever his service. A season whose standings lack a club that has WAR has no measured replacement level, never one from the clubs that remain. A club-finance history row whose every money field is zero is a placeholder, read as unknown, never as $0; a value whose meaning the export does not establish (the market scale, owner expectation) is shown as exported and never interpreted. A market snapshot is written once per import key (save, league, game date): re-running the same import writes nothing new, and a snapshot that cannot be written never fails the import. Payroll lists every player it counts. The club's marginal value of a win moves with its competitive position and never with its philosophy (phase 5b, row below). **Hardening (B-13, D-17, D-14):** a season's WAR prices a full season's salary only in proportion to the schedule it covered: a short prior season (a 60-game season under a 162-game schedule) is scaled to the full schedule, never read as a full season, and a season, or this season's pace, resting on less than the stated minimum share of the schedule is not used, with the reason. A basis resting on fewer market contracts than the stated minimum is not computed, and when no two bases can be, the price is `unknown` with the reason, never a point. A league that runs no financials shows no club money in dollars: budget, payroll, revenue, expenses, cash and media money are `unknown` with that reason, never the figures the export still carries and never $0. "This season" is read from the league's own row where it states it, so a minor league whose parent chain is broken still knows its season, while its contract rules stay `indeterminate`. |
| `playerValueProduction.test.ts` (3a and 3b built) | **3a, results:** a player with no major-league results in the projection window and no ability evidence is `unknown`: never zero, never a league average, and minor-league WAR never stands in for it (Q-9). A missing age, WAR or share of the season played leaves production `unknown` with the reason, never a midpoint. His rate band (wins per opportunity: what is not known about his rate plus the drift of talent) is at least as wide in every season further out as in the one before. A player whose expected playing time declines may have a narrower wins band, but never a narrower rate band: the wins band is rate times expected playing time with the playing-time uncertainty, and it follows playing time down. Removing a season of results, or reducing its plate appearances or batters faced, never narrows the rate band, and never narrows the wins band on the same expected playing time. Thinner usage evidence never narrows the playing-time band relative to its central. A better visible line, all else equal, never lowers expected wins, nor the high edge (phase 3b: its low edge may move down, because a better player is expected to play more and uncertainty about that playing time widens the band both ways). **A better player is expected to keep more of his playing time than a worse one of the same age and usage** (supervisor, 2026-09-23: playing time conditional on quality, attrition explicit). A player outside the organization is valued on exactly the same terms as the same evidence inside it. The current partial season counts in proportion to its playing time, and what he has banked this season is a fact beside the band for the rest of it. A two-way player's band is the sum of both sides, edge with edge. Known days out are a fact (owner, 2026-09-23): they come off his expected playing time, so the central moves down, while the band keeps the high edge of an earlier return and its low edge moves down; a career-ending injury puts nothing-at-all inside every season's band and moves every later central to nothing (superseded 2026-09-23: "a stated injury only widens"). The 50% band sits inside the 80% band, for wins and for the rate, and both rate bands widen with the horizon. Each season's band carries the coverage its fit observed on held-out seasons beside the target. Injury proneness never narrows a band, and an unknown proneness is at least as wide as any known one (a missing column, a blank or a 0 is unknown, never normal). The fallback prior is labelled as such and stamped provisional; the save's own fit is stamped with its run record. |
| `playerValueRatings.test.ts` (3b) | **Unknown stays unknown:** a player with no major-league results and no ability evidence is `unknown`, never zero or an average; removing all ability evidence makes the ability component `unknown` (a prospect has no band; a thin record falls back to his results with a rate band at least as wide); his expected major-league playing time is measured on the save (how often players at his level and age reached the majors) or `unknown`, never assumed. **A prospect:** with ratings, a level and an age he has a band for every season of the horizon with its basis (the ratings and their provenance, the arrival evidence, the development path's source, labelled "not yet calibrated on this save" while it is the prior, and the same-time caveat); his low edge includes producing nothing in every season; his rate band is never narrower further out; expected ability moves from current toward potential, and the band widens with the gap between them; a partial `ScoutedAbility` (an unknown potential, glove at his position, running, or one current pitching tool) never narrows the band on the same expected playing time, and two missing current tools leave him `unknown`; a better scouted line never lowers the central; nothing in the projection knows the club (Q-2). **The blend:** results and ratings are weighed by reliability, the weights shown and summing to one, the results' weight rising with his major-league sample; a player with plenty of record is effectively his results alone. **The fit:** the ratings → rate mapping is fitted per save, recorded with its run record, the same-time caveat and the gate's verdict, its slopes never negative; arrivals are measured from minor-league usage by level and age and checked on seasons the fit never saw (a save without minor-league history measures none, and says so); the development path is the provisional prior, labelled, until the save holds enough rating-snapshot pairs a season apart, and then the save's own, automatically; a form of the mapping without the glove or running is never more certain than the full one. **The reader:** a thin record's ratings reach his projection through the adapter, the same evidence on another club valued on exactly the same terms; the minor-league reader returns where and how much a player played, never WAR (Q-9). |
| `playerValueProductionFit.test.ts` (3a and 3b, D-053) | Calibration belongs to the save. A save with no history is not calibrated: the fit is the prior, it cannot be validated and it is not adopted. Thin history shrinks toward the prior by its sample (more history, less prior), serves wider bands and says "not yet calibrated on this save (N seasons)". A fit records its window, its held-out coverage for both bands per horizon and the gate's verdict, and the verdict agrees with the numbers. A completed season with no fit is fitted once and recorded per save; a fit that fails the gate is not adopted, the previous model stays in force and the reason is shown. A re-import without a newer completed season fits nothing; a newer completed season triggers exactly one refit, and the adopted fit is the one in force, stamped by its run record. The ratings model (3b) is fitted once per completed season beside the results fit, and refitted by itself, once, when the save's rating snapshots become enough for its own development path. |
| `playerValueCone.test.ts`, `productionCone.test.ts` (the player card's cone, 2026-09-23) | The card's cone runs from this season to the last controlled season within the production horizon, and marks free agency after it; where control is not established it says so and never invents a status. It carries both bands and the central exactly as production states them, the 50% band inside the 80%, and draws each at its served width, so a fading player's cone narrows and negative wins stay on the axis. Coverage not measured on the save is "not measured", never the target. Unknown production draws nothing and states the reason, never a zero line or an average. Written first; both files failed on the missing join and component. |
| `playerValueProduction.test.ts`, `playerValueProductionFit.test.ts`, `playerValueCone.test.ts` (hardening, 2026-09-23) | **The central is the expected wins.** On a league whose playing time follows a talent that drifts (the players who keep playing are the ones who stayed good), the held-out central is unbiased at every horizon and in every sizeable subgroup (kind, usage third, quality tier, age band): the rate of those who play is fitted apart from the chance he plays, never a product of two expectations. **Playing time has a physical ceiling:** no season's playing-time high edge exceeds the most the save's own history shows a player playing per scheduled game, times that season's schedule, and no wins high edge exceeds that ceiling times the high edge of his rate. **Playing time is relative to the schedule:** in a 60-game league no band exceeds what 60 games allow, and a past short season (60 of 162 games) is read at its own schedule, never as a part-timer's season. **Seasons before the league existed are unknown, not zero:** a first-season regular's next-season playing-time band holds a regular's playing time. **The rest of this season is in-season,** never next season's attrition: a regular at his pace keeps close to it where this season's games measure how much playing time holds, and keeping his pace stays inside the band where they do not. **Known days out are a fact:** they come off expected playing time in each season they cover (the central moves), the band keeps the high edge of an earlier return, the calendar's edges hold (days out before Opening Day or after the last game fall where the calendar puts them), a duration the export states for many players at one value it contradicts (day-to-day or active holders) is not read as days, and a stated injury is always named in the basis, even where it moved no number. **A season lost to injury is never read as evidence of less playing time** (owner, 2026-09-23): with an injury stated this season, a season under a quarter of his best in the window counts as not known, the central reads his healthy playing time and the band still reaches the reading with the lost season. **A listed pitcher's batting is not a hitter's line:** it is never projected as a side and never enters the hitter fit, so the hitter model is the same with or without pitchers who batted. **The same-time ratings never undo the results regression:** until the save measures them as a forecast, a player whose ratings imply exactly his results gets (almost) the results-only rate. **Observed coverage belongs to the estimator served:** a projection that leans on ratings, the rest of a season under way and a horizon between two measured ones are "not measured" or named as interpolated, never another estimator's figure. **The gate cannot pass a miscalibrated fit:** a fit whose pooled coverage is on target but whose regulars' band covers half the time, or whose central is materially and significantly biased in any subgroup with enough cases, is rejected; each horizon's prior weight is its own, a horizon the save has no cases for is labelled and widened as the prior's; a prior fitted on the save's own held-out seasons is not used, and the record says so. **Statistics:** repeating one player's rows never makes an effect significant (standard errors clustered by player, Holm across the family), a proneness playing-time effect applies only where it was measured, a separable logistic is finite and flagged, and a target season with a blank WAR is excluded, never a zero. **The fit store:** a refit that fails the gate never replaces the fit in force; a new save under a reused name and league id, or a fit through a season the league has not completed, is never in force; a season is complete only with its major-league lines; an adopted fit that is mostly the prior is stamped provisional; the refit runs off the server's event loop and never records a result read across an import. **The cone:** a projection from ratings never rests on "Major-league results : 0 PA" and never says "Calibrated on this save" unless every part it rests on is. **The gate evaluates the method as it is served (owner's option C, 2026-09-23):** every held-out origin is scored by the method refitted through that origin, the pooled horizon-1 cases are exactly the origins' own, and one era whose results drift cannot decide the verdict: no origin holds more than a third of the scored cases, and the pooled bias is under half of the drifting origin's. **A league's WAR scale is a unit:** under the prior, the same record in a league at 0.4 of the scale projects 0.4 of the rate and of its band, on the same playing time. **The same-time ratings pull only by their own weight:** they undo at most that share of the results regression. |
| `playerValueSurplus.test.ts` (5; built in 5a, row below) | Sunk salary never favours keeping a player: raising the money already paid, or the guarantee owed whether he stays or goes, never raises his retention margin. A contract's surplus is his production value less his cost, season by season, with every component shown. Surplus bands are combined edge against edge and are never narrower than their parts allow. |
| `playerValueLens.test.ts` (5; built in 5b, row below) | The philosophy lens never changes the neutral value: every neutral figure, band, price, control status and unknown is identical under every philosophy. Every difference between "our view" and neutral names the dimension that made it. The lens cannot turn an indeterminate into a number. Changing philosophy recomputes nothing. |
| `playerValueInvariants.test.ts` (5; the card and the league-wide read in 5a, row below; the other consumers in 6) | A player's value is the same regardless of which consumer asks: Contracts, Payroll, the Trade Center, Free Agents, Org Comparison and the player card receive one identical valuation. Adding or removing an unrelated player changes nobody else's value except through the league price of a win, and then by the price alone. Value never reads the protection tier, and the tier never reads value. |
| `playerValueBoundary.test.ts` (1, widened each phase) | Ratings only through `scoutedEvidence.ts`: the reader alone loads them (ability, splits, running, the glove at his position, the persisted snapshots), the pure ratings modules import its types only, and no value module names a rating column or the snapshot table. No `players_value` field in any value module. No minor-league WAR: the minor-league reader selects usage only and the arrival history carries none. Consumers reach value only through its module. No philosophy in the neutral path. No tier, no defensibility, no prospect decision and no rebuilt roster right. Nothing writes to `league.db`. Every constant is declared once and stamped; the only fitted artefacts in code are the two provisional priors, and the ratings prior measures no arrivals. The ratings refit runs once, after the results refit, in the same guard. (PLAYER_VALUE.md Part 10.) **Hardening (A-16):** the checks read the parsed source, not regular expressions over it: an import in any form (double quotes, `import()`, `require`, a bare package, a subdirectory) is seen, a comment marker inside a string hides no code, every database call's receiver and SQL are inspected whatever the SQL's case or quoting, and each hardened check has been shown to fail on a deliberate mutation. A known violation owned by another fix is listed with its finding ID and must still be present, so the list empties as the fixes land. |
| `playerValueCrossSave.test.ts` (hardening; builder `tests/syntheticSave.ts`) | Whatever the save (a brand-new fictional league, two or three seasons of history, a 60- or 100-game schedule, a reserve clause, no arbitration, no financials, non-MLB rules, a minor-league-only league, a broken parent chain, a low- or high-offense environment, missing optional tables or columns, an off-season or playoff export, two top-level leagues in one universe, a reused save name), every Player Value entry point answers without an exception and without a non-finite number; an unknown carries its reason; a label never claims more calibration than the fit in force has; and no answer is confident where its evidence is missing. A case whose fix belongs to another change is an `it.todo` naming its finding. Since F1 (2026-09-23): a fit is recorded only through a season whose lines exist, and standings beside a season with no lines are not that season's (its share played is unknown, never a whole season played with nothing in it), while the real completion is fitted when its lines arrive; a fit never scored on held-out seasons is not called calibrated; a label names the seasons of lines when none is usable, never "(0 seasons)"; one missing column blanks only its side; an independent league's top level is its majors; a new save under a reused name never inherits a fit. |
| `playerValueControl.test.ts` (hardening F2, 2026-09-23) | **Options.** The season being played is under contract at its salary: its option was decided before it began, and only a future option season shows both branches. An option flag the export does not populate (zero on every contract) is unknown, never "no option": the term's last season says so. A club and a player option on the same season is a mutual option, never the club's alone. **Opt-outs.** A contract with an opt-out never shows the seasons after it as certain: from the season the exported count reads (the term's first season plus the count) each season shows both branches, staying under the deal at its salary or leaving on his Player Rights standing, and says the count's timing is read, not established; an opt-out whose reading has passed or lies past the deal is named and changes no season. **A blank contract row** (no term, no kind, no paying club) is the row the export writes for thousands of players without contract terms, never evidence of a minor-league contract: it has no kind. A player the export places on a major-league club with major-league service takes his Player Rights standing after this season, the blank row named, and a free-agency answer stays `indeterminate` because a deal the export does not carry could still hold him; this season's cost stays unknown. **Service on the injured list.** The major-league injured list accrues service (the export shows every player on it banking the season's clock) and a player on it cannot be optioned (observed), so the projection's low edge adds the days left on his stint, and never says he banks nothing because he is hurt. **The schedule.** A season's remaining service is capped by its schedule: once the last scheduled game is played nothing remains, and each later season adds the service its schedule can bank (the calendar from the first scheduled game to the last), never more than the service year, so a league whose schedule is shorter than the service year never reaches free agency seasons early. |
| `playerValueSuperTwo.test.ts` (hardening F2, 2026-09-23) | **Arbitration trips count winters, not service classes.** After a Super Two year the next arbitration season is his second trip, and a player already in arbitration is never numbered a trip he has already taken; where an earlier winter is not in the export (a Super Two year before this one, or an arbitration year the projection leaves undecided) the trip is a range, never the lowest reading alone. **The cutoff's edges are readings, not bounds** (owner, 2026-09-23): within the policy margin of either edge the year before the arbitration line is `indeterminate`, a tie with the cutoff included, and the margin only ever widens. **A short schedule.** Where the league's schedule banks less than a full service year, whether the 86 days apply is not established, so the window stays `indeterminate`. |
| `payrollControl.test.ts` (hardening F2, 2026-09-23) | **One answer for every consumer.** A next-season option reaches Contracts, Payroll, the Trade Center, the player card and the AI as an option with both branches, never as "signed". "Extended" means next season is the extension, never the current deal running on. A player whose control ends this season is leaving, never `indeterminate`. **Payroll reads Player Value's contract facts**: a salary clause the export does not populate (retained salary) leaves the money owed to departed players not established, said and never $0; a club option season is not guaranteed money and is counted apart from the committed total, and a salary the contract covers but the export does not state is unknown, never $0. **Service** is shown in years and days, never as a decimal of years. **Free Agents** asks the control timeline about every contract: a contract whose next season is an option is undecided between its branches, counted and never silently dropped. **Club finances** on Contracts and Free Agents are Club Finances' figures: a figure the export does not state is unknown, never $0. |
| `playerValueRatings.test.ts`, `playerValueCrossSave.test.ts` (hardening F4, 2026-09-23) | **A prospect not yet called up this season can still be.** His chance of major-league playing time in a later season is measured on the players of his level and age in the condition he is in (not yet called up at this point of the season), never only on those passed over for the whole of it: at the season's start his chance next season is the rate at which such players played the next season, and part-way through it the players called up later in their season still count, in proportion to the season still to play. **A better prospect is likelier to arrive and plays more when he does** (the principle of the results path): his chance and his playing time move with his projected quality by the save's own measured effect of quality on keeping major-league playing time, and the players of his level and age together keep the chance and playing time the save measured for them. A prospect projected below replacement is not expected to take an average arrival's playing time, so weaker players filling his level do not make his central more negative; his low edge still includes producing nothing. **The arrival gate reads relative miscalibration:** a fit whose held-out chance (or expected playing time) is off by a material share of what happened, beyond three standard errors clustered by player, is not adopted, however small the rate: a fit that keeps predicting well above what happened never passes (since hardening F5 the errors are clustered by origin too, so the miss must persist across origins rather than be one era's); the absolute tolerance still applies. **Two top-level leagues:** a league's arrival rates are measured on its own affiliates' players, never on another league's farm (whose players would count as never arriving), and reaching any top-level league is arriving. **Partial ratings widen a thin record too:** a missing glove, running or current pitching grade spans the scale's ends in the blend with his results, so his band contains the band of every complete reading consistent with what is known, never a midpoint. |
| `playerValueRatings.test.ts` (hardening F5, 2026-09-23; the owner's option C applied to arrivals) | **The arrival model is judged the way it is served.** Every eligible origin season (from the window's start + 5 to the season before the last, at most 8, evenly spaced) is scored by the method fitted through that season, projecting the next season's minor leaguers; a horizon of an origin is scored only where that fit rests on the gate's minimum cases from at least three origin cohorts, so no horizon is judged on a fit thinner than any served one. **Recent seasons count for more:** in a league whose arrival rates changed some seasons ago, the chance served is nearer the recent seasons' rate than the whole window's average (a half-life of two seasons, the results fit's). **One era's swing is not a bias:** the gate's standard errors are clustered by player and by origin, so a league whose arrival rate swings from season to season around a steady level is not failed by the swing, while a model that keeps missing in one direction still fails; the tolerances are the tightened gate's, never loosened. **A snapshot is read at its point of the season:** the arrival chance a rating snapshot is compared with, for the chance by potential, is that of a player not yet called up at that point of his season, never the season's start; where that point is not established, the expected chance is the range between the season's start and its end, and a tier's effect is used only if it holds across the range. **An unknown says what it rested on:** a player with no major-league results whose production is unknown names its source (his scouted ratings when his ability was projected and his playing time was not; none when nothing could be projected), never a blank. |
| `playerValueCone.test.ts` (hardening F2, 2026-09-23) | The cone's control follows the timeline: an opt-out season and an extension season each have their own label and code, never merged with the current deal. "Free agent after" names every season after which he may be free: where the last controlled season lies between arbitration and free agency, the mark names the season before it too. |
| `playerValueRatings.test.ts`, `playerValueCone.test.ts`, `productionCone.test.ts` (hardening F6, 2026-09-23; the owner's option (b)) | **The arrival model is adopted horizon by horizon.** A horizon is served only when its own held-out check and the check of every horizon before it passed the gate, whose tolerances are unchanged: a horizon that passes after one that failed, or after one that could not be checked, is not served, and nothing is served unless the next season passes. **A season beyond the last served horizon is not established, season by season,** each with a reason naming what the gate found at that horizon; the seasons before it keep their full bands, and nothing is carried forward, extrapolated or averaged into the later ones. **A total over seasons that include one not established is not a number:** it names the seasons it cannot include and is never a sum that reads them as zero. **The cone draws the established seasons and marks the later ones not established,** with no band there and each season's control still shown. **A label never claims more calibration than was measured:** a partly adopted arrival model is "calibrated through N seasons out", never plain "calibrated". |
| `playerValueCost.test.ts`, `playerValueCrossSave.test.ts`, `payrollControl.test.ts`, `playerValueCone.test.ts` (phase 4a, the cost of controlled seasons) | **A pre-arbitration renewal costs from the league minimum to the save's own renewal pay:** the band starts at the minimum the league's rules state (a missing minimum is unknown, never another league's), and its high edge is measured on this import's pre-arbitration one-year renewals, never assumed to be the minimum. **An arbitration season is never assumed to cost the league minimum and is never a point:** its band lies above the minimum (reaching it only where the save shows the class paid the minimum at a platform as low as his, and then saying so; phase 4a review), from the save's own arbitration ladder (what the class is paid against its platform production), the season's projected platform production, edge against edge; a line measured on the save's own contracts is in the save's own dollars, so the price of a win's band never widens it a second time (only the provisional prior's shares carry it). **The ladder is read through Player Rights:** a contract is an arbitration salary only when Player Rights finds its holder in arbitration this season, its class is Player Rights' count of his trips, and an arbitration-class one-year contract at the league minimum is not read into the class's line (pay there is held at the floor, not set by the platform) and is never named a transaction the export does not show. **A range is covered, never guessed:** where the arbitration year is a range the band covers every year in it; where Super Two or eligibility is indeterminate the band covers each status he could be; a season that may be free agency is priced only as what he costs if the club holds him, and says he may leave. **Thinner evidence widens, never narrows:** the same pay measured on fewer contracts gives a band at least as wide; a wider production band never narrows the cost band, nor a wider price band where the prior is in it; a class below the policy minimum is the provisional prior widened by the save's own cases, and says so. **A league with no arbitration rule never gets MLB's ladder:** no arbitration, an arbitration rule the export does not state, or a thin class in a regime that is not MLB's leaves the season's cost unknown with the reason; the prior (the imported real-world contracts) applies only where the regime as read is MLB's. **Unknown stays unknown:** a platform season whose production is unknown or not established, an unknown price of a win or a league without financials leaves the arbitration cost unknown with the reason, and a reserve-clause renewal stays unknown until the save measures renewal pay. **Committed payroll never includes a projected salary:** Payroll shows the projected pre-arbitration and arbitration cost as a band beside the committed money, never inside its totals or its headroom. **One cost for every consumer:** Payroll, Contracts and the card's cone show the season's cost exactly as the timeline serves it. |
| `playerValueCost.test.ts`, `playerValueControl.test.ts`, `playerValueCrossSave.test.ts`, `payrollControl.test.ts`, `playerValueCone.test.ts`, `productionCone.test.ts` (phase 4a review, 2026-09-23) | **A thin class's own contracts widen its band, never narrow it, and never set a slope:** below the policy minimum an arbitration class is the provisional prior's reading hulled with the range the save paid the class above the minimum; one, two and three contracts at extreme pay each widen the band at least as far as the one before, a class's own handful of contracts never has a line fitted and extrapolated through it, and a band never runs far past what any contract in the league was paid on the strength of a few cases. Where no prior applies (the regime as read is not MLB's) a thin class stays unknown with the reason. **One contract cannot move a rung:** the class's line is read robustly, so one star or one market contract read in the class moves the line no further than any single case can move a median; its spread and its own uncertainty come from the same fit. **Every priced season has a central inside its band:** the class's line at the platform's central production (at the price's central where the prior is in the reading), or the save's median renewal; a season between statuses Player Rights leaves open names each status's central and chooses none, and a season that may be free agency is central "if held". Payroll shows the sum of centrals beside the summed range. **The band says what it is:** Payroll, Contracts and the card call it a range of reasonable readings, edge against edge, never "expected" and never an interval; "if held" is shown with the figure, not only on hover; a band never prints as a point ($780K–$790K, not $0.8M); no raw constant, "90% point" or Super Two clause for a player who is not one reaches the text, and a band resting on the prior alone never mentions a save line. **The minimum is a reading, never an assumption:** where the save shows one-year contracts at the league minimum in the class, a season whose platform reaches as low as theirs has a low edge at the minimum and says so; elsewhere the band stays above it. **The basis names only what the band used:** the price of a win's band is named only where the prior's reading is in the band. **The save's own line needs no price of a win:** it is in the import's dollars, so an unknown price leaves only the prior's readings unknown. **A branch the player decides is never a certain cost:** a player option declined, a mutual option declined or an opt-out is priced as what he costs if held, and says the player decides; wherever an option is shown, its declined branch's cost is shown beside it. **A Super Two's later seasons are priced in the class the ladder measures such players in** (the class his service puts him in) as well as by his trip. **A league with no arbitration never lists arbitration** among the statuses a season could be (Player Rights). **A platform beyond what the class was measured on says so**, and the band is never capped to hide it. **One cost per season:** a reading computed without production prices no controlled season, so no consumer serves a second, different cost. **The renewal band's text says what it computed:** where the renewals are too few for the upper bound to leave any out, it says the bound is the largest renewal. |
| `playerValueSignings.test.ts`, `playerValueCrossSave.test.ts`, `playerValueFinances.test.ts`, `playerCard.test.ts` (phase 4b, the measured price of a win across imports) | **Each import records the save's own contracts, once:** every contract its clubs hold (term, club, organization, placement), Player Rights' standing for this season and the two after it, the player's expected production at the time and next season's cost, keyed by the save's identity and the game date; a second capture of the same import writes nothing, and a new save under a reused name never reads another save's contracts. **A snapshot difference names what changed, never a transaction the export does not give** (D-020): a contract whose first season, length or club changed between two imports is an observed change; a club change on the same terms is the contract moving with him, never a signing; a controlled player no club holds at the later import was not tendered or was released, and the reading says the export does not say which. **A change is read through Player Rights at the earlier import:** a new deal for a player Player Rights found free-agency eligible for its first season, with a club that did not hold him, or when no club held him, is a free-agent market signing; a one-year deal with his club for a player in arbitration is an arbitration salary (award or settlement: the export does not say which), and at the league minimum it is not read as one; a one-year deal before arbitration is a renewal, and under a reserve clause a reserve-clause renewal; a longer deal with his club while he is controlled, or a new deal over seasons his contract still covered, is an extension. Where the standing was indeterminate or the reading is ambiguous (a free agent staying with the club that held him, a controlled player's new deal elsewhere), the change is named, counted and left out of every measurement. **The measured price comes from free-agent signings only:** salary above the minimum over the signed player's expected wins at the earlier import, summed over the signings observed (a ratio of sums), each season of a deal priced only where his production was established then; a deal whose first season was already under way at the earlier import is left out. It rests on at least the opening basis's minimum of contracts; with fewer it is not measured and says how many were observed. **Its band is its signings resampled, and the opening band is compared like for like:** each opening basis carries the same resampled band over its contracts, and the band a measured price must beat is the opening's spread with that sampling in it; fewer signings at the same prices never give a narrower band. **The measured price replaces the opening one only when its band is narrower** (Q-4, no fixed count): while it is wider, not measured, or no off-season has been observed, the opening price stays in force and says why, naming the signings observed and both bands; every import's opening and measured readings, and which was in force, are kept and served. **No off-season observed is said precisely:** a save with one import, or with imports inside one season, says the measured price needs two imports across a winter and how many it holds; nothing is measured from one import. **Observed arbitration salaries test the ladder:** each is set against the band the earlier import priced for that season, and the coverage is reported with its count; once a class holds the policy minimum of observed salaries, they are a reading of that class beside the import's own cross-section and the season's band covers both. A league without arbitration has none to score and says so. **Reserve-clause renewals observed across imports price a reserve-clause season** from the league minimum to the save's own renewal pay, measured like the pre-arbitration renewal spread once the policy minimum is observed; until then the season stays unknown and says how many were seen. **Replacement is measured from freely available talent:** players who joined a club for nothing (a minor-league deal, or a major-league deal at the minimum, from outside the organization, with a major-league record) are read by their major-league WAR per opportunity for the club that took them; below the policy minimum the export's WAR convention stays, labelled provisional, and once measured the measured price counts a signing's expected wins above that level and says so. **Payroll shows which price is in force and why, and the price's history** across imports. Nothing here calls a signing good or bad or ranks a player: it describes, never authorizes. |
| `playerValueSignings.test.ts`, `playerValueCrossSave.test.ts`, `playerValueFinances.test.ts`, `playerCard.test.ts` (phase 4b review, 2026-09-24) | **A winter is read by the calendar, not by the season number.** An import taken after the league moved to the next season but before its Opening Day is inside the winter: a deal for that season signed then, or seen at a later off-season import, is a market price, never "a season already under way", and last season's standings beside the new season number are never games played in it. Several imports across one winter count one winter; imports a winter or more apart say so, and what they show is not priced as one winter's signings. **An extension already signed moves with the player:** the extension the earlier import held taking effect is never a new signing, whether or not he changed clubs in between. **A club that held him during the season is not the open market:** a free agent signed by an organization his lines show held him during the season before is counted and left out, like one re-signed by the club that held him at the earlier import; where his lines cannot be read he is left out and it says why. **Two timelines are never compared:** an import dated at or before one already recorded (the save went back), or an import of a date already recorded whose season's play differs (a reloaded save played again), starts a new timeline; nothing is compared across it, what the superseded imports observed of the same period is left out, and the observed market says so. **What changed is named for what the export shows:** a term that now ends no later than it did is a term changed within its seasons (an option declined, a buyout, an opt-out or a restructure; the export does not say which), never an extension; a club change between rows with no exported terms says how he moved is not in the export, never "the same terms"; a major-league deal from his own organization after a row with no term is his organization adding him; a player no club in this league holds is described as such, never as held by no club anywhere. **The measured price is read like the opening one, and never replaces it by changing what it measures:** it is a set of bases, each with its sampling: per win projected at signing over the deal (a deal running past the projection's horizon is left out of that basis and counted), per win projected in the first season, per win expected in the first season if he plays, and, once the first season is completed, per win he actually produced in it, on its schedule's footing (the opening's own unit). Its band is the spread of those bases with each one's sampling, resampled by winter once two winters are observed, so a price that moved between winters is not shown as a precise one. It replaces the opening price only when it holds the realized reading, its signings cover each third of the winter's free-agent class by expected wins, and its band is narrower than the opening band with its sampling; a winter of cheap deals alone never sets the price. The reason names the unit (per projected win at signing, per realized win) and flags a measured central below the opening's floor or outside its band. An opening band whose sampling has no upper edge is wider than any bounded band, and a basis whose resampling fails is unbounded, never dropped. **Measured replacement never changes the price's unit:** until surplus applies one level to both sides, the price, the cost ladder and production stay in the export's WAR; replacement from free talent is measured and shown with its band, who it rests on (only pickups who played) and how many did not play. **Observed awards show how sharp the ladder is, and join it only where they belong:** coverage is reported beside the width of the bands scored; an award whose trip is a range or unknown is counted and left out of the class readings; a trip beyond the top class is read in the top class, as the ladder reads it; once a class holds the minimum of observed awards they join its readings and its cost says it is measured in part. **Recording an import reads only the import before it:** what the earlier imports observed is kept as it was observed, so recording and reading the market do not grow with the number of imports held. |
| `playerValueSignings.test.ts`, `playerValueCost.test.ts`, `playerValueCrossSave.test.ts`, `payrollControl.test.ts`, `playerRights.test.ts`, `playerCard.test.ts` (phase 4 owner decisions, 2026-09-24) | **The price of a win in force is per win produced** (owner, 2026-09-24): once measured, its central and its band are the reading per win the signings produced in their first season (the opening's own unit), and nothing else: a reading per win projected at signing is never the price in force, never part of its band and never its central; it is shown beside the price as a check on the projection, with its ratio to the price. Until the realized reading exists the measured price is not in force and says it waits for a first season to be completed, the projected readings still shown as the check. The adoption reason, Payroll's price line and the price's history all name the price per win produced. **Payroll combines players as independent, and says so:** the club's range around the sum of centrals combines each player's own distance from his central, on each side, as independent across players (never every player at his edge at once); what is not noise stays at its edges and is added, never combined: a season Player Rights leaves between statuses, a range of arbitration classes, and a season he may not be held (a player who may leave adds nothing to the low edge). The combined range always lies inside the edge-to-edge range and always holds the sum of centrals; for one player it is his own band; no player's own band is ever narrowed; and Payroll labels it "players combined as independent; not a calibrated interval" with the edge-to-edge sum still shown in its details. **An arbitration salary never falls** (owner-attested, 2026-09-24, a rule of the game as the owner attests it, stated by Player Rights): every season priced as arbitration (the arbitration branch of a season between statuses included) has a low edge, and a central, at least the player's previous season's salary where that salary is known (this season's contract salary for next season; for a later season the previous season's low edge, so a held player's arbitration low edges never fall from one season to the next); where the previous salary is not known the rule cannot bind and the basis says so. It is what he costs if tendered: a non-tender stays possible and is said. It is never MLB's 20% rule. An observed arbitration salary below the player's previous salary is flagged as contradicting the owner-attested rule, counted and named on Payroll's awards line, never silently absorbed. **The contract history is kept at the winters:** a full contract snapshot is kept only for an import that brackets a winter (an endpoint of a pair of imports across one) and for the most recent import; the others' snapshots are removed when a later import is recorded, after its pair is stored, never across save identities, never the latest, and never one a stored pair still needs; every stored pair and timeline event is kept, and the measured price, the awards and replacement read the same after pruning as before. A pruned import is never read as an import with no contracts. |
| `playerValueSurplus.test.ts`, `playerValueInvariants.test.ts`, `playerValueBoundary.test.ts`, `valueSection.test.ts` (phase 5a, 2026-09-24) | **Sunk salary never favours keeping a player.** Raising the money already paid (this season's salary for the part already played, a past season's salary) or the guarantee owed whether he stays or goes never raises his retention margin; it lowers his contract surplus, never the margin. A contract can have a deeply negative surplus beside a positive retention margin (the money is gone either way), and where every cost exists only if he is kept the two views agree. **A contract's surplus is his production value less his cost, season by season, with every component shown:** the wins band, the price band and which price is in force (the opening one or the one measured per win produced), the cost band with its basis, the discount weight and the seasons included. **The rest of this season counts only its part still to be played:** his wins for the rest of it and the same share of his salary and of a replacement's minimum; what he has banked and the salary for the part played are shown and never counted. **Bands are combined edge against edge** (low wins at the low price less the high cost, and so on) and are never narrower than their parts allow; the central comes from the components' centrals and lies inside the band. **Unknown stays unknown:** a season whose wins or cost is unknown or not established has an unknown surplus with its reason, and a sum over it is not a number: it names the seasons it cannot include, never reading them as zero. Without dollars (no financials, no price of a win, no minimum) the value is in wins only and dollars are unknown with the reason. **The neutral discount is one stated policy rate** (5% a season, owner 2026-09-24): a season *s* seasons from now weighs 1/1.05^*s*, this season's remaining part 1, and the price of a win is held flat. **An option season is both branches:** the contract surplus covers the option exercised and declined and chooses no central between them; the retention margin is the branch where he is kept; a season the player decides, or one he may leave in, is "if held". **One level of replacement on both sides:** his wins are above the export's replacement level, the replacement's wins are zero there, the price is per win above that level, and a replacement costs the league minimum; the measured replacement is shown, never applied. **No verdict:** no view says keep, release, trade, extend or sign. **One valuation whichever read asks:** the card's route, the one-player read, Payroll's players and the league-wide read give the same surplus; adding or removing an unrelated player changes nobody else's, and a market contract added changes another's only through the league price of a win, and then by the price alone. Value never reads the protection tier and the tier never reads value. **The card shows both views side by side** with their components and the basis, and what is unknown says so. |
| `playerValueLens.test.ts`, `playerValueWinValue.test.ts`, `playoffs.test.ts`, `playerValueBoundary.test.ts`, `valueSection.test.ts` (phase 5b, 2026-09-24) | **The lens never changes the neutral value.** Under every philosophy (any dimension from 0 to 100, any policy), every neutral figure, band, price, cost, control status, discount weight and unknown is identical to the neutral valuation handed to it, which it never alters; "our view" is served beside it and always states the neutral figure it started from. **A philosophy with every dimension in the neutral band and the default policies leans on nothing:** our view is the neutral view exactly, with no lean and no note. **Every difference between our view and neutral names the dimension or policy that made it**, its value, what it did and by how much; a dimension read but not leaning says why (in the neutral band, or no season it applies to). **A lean only re-weights and re-reads what is known:** the competitive window re-weights the seasons against the neutral 5% (a win-now club discounts later seasons more, a club building for the future less; this season's remaining part always weighs 1); risk tolerance reads each season's band from its centre toward its low edge, never below the low edge and never above the centre; team control weighs the seasons the club controls at its option (pre-arbitration, arbitration, the reserve clause, a club option) more or less; cost efficiency weighs his cost against his production; payroll flexibility weighs guaranteed salary in later seasons, in the contract view only. **Sunk salary never favours keeping a player in our view either:** money owed whatever the club does is in neither future of the retention margin under any philosophy, so raising a guarantee changes our retention margin no more than the neutral one. **The lens cannot turn an unknown into a number:** a season or a sum that is unknown in the neutral view is unknown in ours, with the same reason. **The policies only word emphasis** (aging contracts, arbitration extensions, rentals, salary dumps): a note names the policy and the seasons or facts it points at, and no number moves. **Changing philosophy recomputes nothing:** the lens reads the neutral valuation as handed to it and reaches no production, cost, fit, table or rating. **The lens never reads the protection tier, defensibility, or the club's value of a win.** **The club's value of a win is a club fact, not philosophy:** from the deadline read's odds model, how much one more win moves this club's chance of the postseason now (and what one to five more, or one to three fewer, would do over the rest of the season), in playoff odds, never in dollars (Q-6), with its basis; the same whatever the philosophy. A club far out of the race gains almost nothing from a win, a club in a tight race gains the most, and a club whose place is beyond reach gains nothing; before a game is played, with no standings, with no games left, or where the club's place in the race is not established, it is unknown or not applicable with the reason, never a default. It never enters the neutral surplus or our view. **A division leader's cushion is measured against its nearest rival**, never against the club at the bottom of its division. **A division leader who is caught still has the wild-card route:** in a league with wild cards, his chance of reaching the postseason is measured against the first club outside the playoff field, so a leader level with his division rival is never read as a coin flip for the postseason (supervisor, 2026-09-24). **The card says it plainly:** the Value section's visible words carry no jargon ("central", "retention margin", "edge against edge", the export's replacement level); each figure's explanation is in its hover; our view sits beside the neutral figure with its leans as short phrases, and the club's value of a win is labelled as context from the standings, never part of the value. |
| `contractsPage.test.ts`, `playerCardHeader.test.ts`, `player.test.ts`, `extensionForm.test.ts`, `dashboardHome.test.ts`, `playerValueBoundary.test.ts`, `evidenceBoundary.test.ts` (phase 6a: the card's header and Contracts, 2026-09-24) | **The card never shows a hidden value figure.** Its header carries no Value or Talent percentile and no Overall or Potential read from `players_value`; the one scouting figure it shows is the organization's own (the scouted tools through `scoutedEvidence.ts`, now and at their ceiling), and a missing grade leaves it unknown, never OOTP's figure in its place. **The header says what the Value section says.** His contract in a phrase (this season's salary, how long he is signed, what happens after this season) and the section's headline (contract value, most likely and what it could be) are the same valuation the section below is served; where he is not valued it says so in one short line. **Contracts shows no verdict.** No player is told to be extended, re-signed, kept, let go, released, moved or held off, and no percentile of OOTP's value is shown or sent; the page describes each contract (its salary, its term, his service, what happens after this season and when control ends), next season's cost and his seasons' cost path exactly as the control timeline serves them, his expected wins, his contract value and the value of keeping him exactly as the card's Value section is served, and our view under the club's philosophy exactly as the lens reads it, each with its basis on hover. **An unknown stays unknown on Contracts:** a figure the save cannot establish is a short word with its reason on hover, never $0 or a midpoint, and it sorts after every known figure whichever way a column is ordered. **The GM sees how current it is:** Contracts and the card's header say the game date the export is from, and say when it is behind the save (the figures that depend on it are then not established) or could not be checked against it (the limitation Player Rights attaches). **The page talks like a front office:** no percentile, recommendation or method word (central, retention margin, surplus, edge against edge) in its visible text; the explanations are in the hovers. |
| `playerValueTrade.test.ts`, `tradeAnalysis.test.ts`, `tradeCenter.test.ts`, `tradingBlock.test.ts`, `evidenceBoundary.test.ts`, `playerValueBoundary.test.ts` (phase 6b, 2026-09-24) | **A trade is read on contract value, each player's as every read serves it:** each player in a deal carries the contract value and the value of keeping him that the card shows, unchanged and never narrowed, with his control season by season and its cost, and his expected production; a trade moves his remaining salary with him, so the sides are summed on contract value, never on the value of keeping him. **The difference between two sides is a band with its components, never a point, a single score or a verdict** (owner, Q-8): what comes in less what goes out, most likely (or the range of its readings where an option, a status or whether he stays is open) with the range it could be, the band always containing the most likely, each player's signed part named, and no word that says who wins, who loses, accept or reject. **Players are combined as independent, and it says so:** a side's range (and the difference's) is the sum of the players' most likely readings with each player's own distance from it, on each side, combined as independent across players, while what is not noise (an option, a status Player Rights leaves open, whether he stays) stays at its edges, added; it lies inside the every-player-at-his-edge sum, which is kept beside it, and for one player it is his own band; labelled "players combined as independent; not a calibrated interval". **Unknown stays unknown:** a player whose value is not known is listed with his reason and left out of the sums; adding him never changes the known sum, and the sum names him; a side with no player valued has no total and the difference is not a number, never a zero. A league without finances reads the whole deal in wins. **The same deal reads the same whoever looks:** the neutral figures are identical under every philosophy and for every viewing club (a contender and a seller read the same numbers); our view leans only through the lens, named per player with its amount; the club's value of a win is context from the standings, never part of any figure. **No hidden score:** the trading block and the trade fits carry no `players_value` figure; what they order by is a shown, explained fact (expected wins), unknown last and never read as zero. **The AI explains the deterministic analysis:** its trade context carries Player Value's decomposition, the bands and the difference, never `players_value`, a percentile or an OOTP rating, and the desk is told never to produce a valuation number of its own and to give no accept-or-reject verdict. **The page says it plainly:** no jargon or verdict word in its visible text ("central", "retention margin", "edge against edge", "win the trade", "lose the trade", "accept", "reject"); explanations are in the hovers. |
| `freeAgents.test.ts`, `aiValueContext.test.ts`, `pageFreshness.test.ts`, `evidenceBoundary.test.ts`, `playerValueBoundary.test.ts`, `payrollControl.test.ts` (phase 6c, 2026-09-24) | **Free Agents shows no percentile and no signing verdict.** No free agent carries a percentile of OOTP's value or any figure from `players_value`, and nothing on the page or in its payload tells the GM to sign, target or pass on anyone; what it shows is what Player Value knows: his expected wins (the rest of this season and next, most likely with the range they could be), his scouted tools now and at their ceiling through the evidence boundary, his age, and what a season of his production costs at this league's market. **A free agent's value is his production at the market, never an asking price:** a player no club holds has no contract, so he has no contract value; the page shows the league minimum plus his expected wins next season × the price of a win in force (the same production value the contract value is built on, one level of replacement, the price held flat), a band with its basis, never a single figure and never what he would ask or be offered. **Unknown stays unknown and sorts last:** a free agent whose production is not established shows it in one short word with the reason on hover, never zero, and sorts after every known figure whichever way a column is ordered; where the league has no dollars, his market figure is not known and says why. **No hidden cut and no hidden score:** everyone Player Value finds reaching free agency after this season is listed (no value cut-off decides who appears), in an order the page states (expected wins next season, most first, not known last); the club's thinnest positions are named by the expected wins of its best player there, each figure shown, and a position with nobody valued is named apart, never read as zero. **The AI explains the same figures:** the briefing's and the staff chat's context carry Player Value's figures (the same totals the card and Contracts show, and the free agents' market figures), never a `players_value` figure or a percentile, and the prompts no longer explain one; the assistants are told to quote the ranges, produce no value number of their own and give no verdict. **The GM sees how current the data is on Payroll, Free Agents and the Trade Center,** as on Contracts: the game date the export is from, and a warning when it is behind the save (the service-dependent figures are then not established) or could not be checked against it. **The pages talk like a front office:** no percentile, jargon or verdict word ("sign", "must", "target", "should") in Free Agents' visible text; the explanations are in the hovers. |
| `rosterScouted.test.ts`, `lineupEvidence.test.ts`, `orgComparison.test.ts`, `evidenceBoundary.test.ts`, `playerValueBoundary.test.ts` (phase 6d, 2026-09-24) | **The Roster shows the organization's scouted view, never OOTP's Overall or Potential.** Its scouting column is the scouts' tools averaged now and at their ceiling on the 20–80 scale (the card header's "Scouted" figure, through the evidence adapter); a grade that is missing leaves it "not scouted", never OOTP's figure or a midpoint in its place, and no roster payload carries a `players_value` figure, even when one is planted. Sorted by it, a player who is not scouted comes after every scouted one whichever way the column is ordered. **The lineup reads every rating through the scouted-evidence adapter, and its choices are unchanged where the evidence is the same.** A bat is what his scouted hitting tools against that hand of pitching say (the calibrated tools model); a glove is his revealed grade at the position; OOTP's own offensive value never decides who plays or where he bats, so changing it changes nothing on the card. Where the evidence is the same the card is the same: the gloves, the tools and who is available are read exactly as before, and every difference from the old card is one where OOTP's valuation and the scouts' view of a bat disagree. A bat with no split grades is read on his overall grades and says so; a hitter with no graded bat at all is named and never ranked as though his bat were average or zero. **Org Comparison ranks nothing by a hidden score.** Each organization's figures are Player Value's as served or objective facts (its record, the major-league roster's expected wins for the rest of the season, the farm's expected wins next season, the roster's contract value, OOTP's payroll and budget), each sum equal to the sum of its players' served figures, combined as independent the way Payroll combines players, and labelled so; a player whose figure is unknown is named with his reason and left out, and the sum says so. The page orders clubs only by a column it shows, unknown last either way, sends no rank and no `players_value` figure, highlights the viewer's club and says how current the data is. **The pages talk like a front office:** no Overall, Potential, "offensive value", `players_value`, rank or method word in their visible text; the explanations are in the hovers. |
| `evidenceBoundary.test.ts`, `payrollPage.test.ts`, `freeAgents.test.ts`, `aiValueContext.test.ts`, `pageFreshness.test.ts`, `tableHovers.test.ts` (phase 6e, 2026-09-24) | **No module reads OOTP's hidden value figures, and none ever can again.** No server module, client module, script or desktop module reads `players_value` or names one of its figures (overall or talent value, Overall or Potential, a percentile of them); the boundary lists no module allowed to, and a module that starts is a failure, never an addition to a list. **Payroll talks like a front office:** its visible text carries no method word (central, band, edge against edge, combined as independent, calibrated, ladder, class, prior, provisional, "if held"); a projected cost reads "most likely $X · could be $A to $B", a season he may leave "if kept", and every figure it showed before is still on the page. **The basis is still shown:** how the club's range combines players, the every-player-at-his-edge sum, how a controlled season is priced and what the price of a win rests on are in the hovers and breakdowns, never dropped. **The trading block is read on the export's own freshness,** like the Trade Center: an export behind the save leaves what turns on service time not established, and it says so. **Free Agents lists the players who might reach the market:** everyone the control timeline leaves between staying and free agency after this season (an option or opt-out whose declined branch is free agency, a season whose status is not settled and may be free agency), each with its reason on hover, in the same columns and order as the other lists, unknown last, never mixed into the free agents after this season and never with a verdict; a player who stays under the club's control whichever way an open question goes is not on it. **The briefing points, it does not instruct:** its last section is "Worth a look this week", worded as something to look at, never "Recommendation of the Week" or an instruction (D-001). **A hover near the bottom of a table that scrolls in its own box opens upward**, so the scroll box does not cut it off. |

## Player card

| File | What it protects |
|---|---|
| `player.test.ts` (the card header, 2026-09-23) | A position player is labelled by his position, never by a pitcher-assignment code: OOTP writes one (11 starter, 12 reliever, 13 closer) on hitters who have never pitched, so a shortstop is "SS", not "RP". A pitcher is labelled by his pitching role. Whether a man is two-way is read from what he has done this season, on the same test the staff and lineup pages use (`twoway.ts`), never from that code: a position player who has pitched a real amount is marked two-way under his position and given no pitching role, and a pitcher who has batted a real amount is marked two-way and keeps his pitching role. |
| `playerCardHeader.test.ts` (the header's contract and value, Player Value phase 6a, 2026-09-24) | The header never shows a figure the organization cannot see: no Value or Talent percentile and no Overall or Potential from `players_value`, even when one is planted there; its scouting figure is the scouts' own tools through the evidence boundary, and a missing grade reads "not scouted". His contract is read from the contract facts (a salary the export does not state is unknown, never $0), and his contract value is the Value section's own total. It says the game date the data is from, and when that data may be out of date. |
| `storylineLevels.test.ts` (the storyline briefing, 2026-09-23) | The same rule in what the AI is told: a position player among the club's pitching leaders is sent with his position and no pitching role, and a listed pitcher keeps his. |

## Pennant for Mac: the presentation layer

Written at milestone N0 of the SwiftUI rebuild, before any code (D-055 to D-060, the D-052 amendment of 2026-09-25,
[SWIFTUI_REBUILD.md](SWIFTUI_REBUILD.md)). Built so far: the sort cases (as `sortCases.test.ts`, N3),
`v2Contract.test.ts` and `severity.test.ts` (N4); the rest arrive with their milestones. Each invariant becomes an `it` in the named file in
the milestone that builds its concern, is built from the synthetic save (`tests/syntheticSave.ts`) or synthetic evidence,
and must fail first for the reason expected. The presentation layer only says what the specialists already decided, so no
case here is a new baseball judgment: each keeps an existing doctrine true once the server writes every sentence.

| File (milestone) | What it protects |
|---|---|
| `v2Contract.test.ts` (N2, N4) | Every visible string a v2 response carries (`text`, `hint`, `display`) passes the one banned-jargon list, and a help tag is at most about 75 characters. A claim always carries its basis; "not known" is a list of sentences, and an empty list means nothing is missing, never "not checked". A philosophy lean is stated beside the neutral reading or is `null`. An OOTP game date is served as the export wrote it, never re-typed as a calendar date. |
| `v2Sort.test.ts` (N2, shared with Swift Testing through `contract/fixtures/sort-cases.json`) | An unknown sorts last whether the column is sorted up or down; an unknown is never read as zero or as the worst value. |
| `clubProfile.test.ts` (N6) | A club's place on a dimension counts only clubs that have the statistic, and ties are stated, not broken silently. A club missing the statistic is left out of "of N", never placed last. Before 20 games a dimension reads "too early" and is neither a strength nor a weakness. A strength is the top fifth and a weakness the bottom fifth, stamped as policy. No dimension is combined into a club grade. |
| `rosterMap.test.ts` (N6) | A holder's place at his position is stated with how many clubs' ranges overlap it; two holders whose ranges overlap are not said to differ. A club whose holder is not valued is not placed and is named as left out. The farm's next man and his readiness are Player Development's and Minor League Operations' answers as served, and control is Player Rights' answer; the map adds no judgment of its own. |
| `horizon.test.ts` (N6) | A prospect sits in the pipeline lane with his readiness range and is never placed in a season. A season whose control is unknown reads unknown, never "free agent" and never "controlled". |
| `severity.test.ts` (N4, Stage A) | A department's severity reaches the desk's common scale (critical, attention, noted) and is never raised on the way: Major League Ops' critical, elevated and watch read critical, attention and noted; a flag the club's philosophy shaded keeps the philosophy-free severity beside it; the farm's scale passes unchanged. A department with no severity of its own gets one by a stated policy line: a running designation or waiver clock is critical with its days left, an option note, a contract date and an injury are noted. Every mapping carries its policy stamp (D-041). A department that could not be read is unavailable, never all clear: its count of items is unknown, not zero, and the reason is a sentence. |
| `frontOfficeLanding.test.ts` (N4, N6) | The landing payload carries no postseason odds, deadline posture or season-window label, and imports neither `posture` nor the playoff odds (D-060). Its masthead is objective facts only. |
| `reportSnapshots.test.ts` (N7) | "What changed since the last export" says what changed between two exports' served figures and never names the transaction behind it (D-020). An item with no earlier snapshot reads as new, never as changed. |
| `desk.test.ts` (N7) | A desk status records the GM's attention and changes nothing else: marking an item handled in OOTP neither resolves the department's finding nor asserts anything happened in the save. Each item names the department and staff member who raised it, and the desk orders by the stated severity it was given. |
| `leagueWire.test.ts` (N7) | A wire entry from OOTP's log says what the log says; an entry from a snapshot difference says the state changed and names no transaction. The wire is ordered by date (followed clubs first when asked), never by a hidden importance. |
| `clubReport.test.ts` (N7) | Another club's players are read through our organization's scouting, the same fog of war as our own; where our scouts have no report, the report says so and fills nothing in. |
| `following.test.ts` (N7) | Following copies the watchlist and leaves it in place. |

## Adding a case

When real-save testing finds a new failure mode, add the case before the fix:

1. State the baseball invariant in one sentence. If it names a player, it is the wrong invariant.
2. Put it in the file for its area (or a new file if it is a new area) as an `it` whose name is that sentence.
3. Build it from synthetic evidence through the pure function that owns the answer (`estimateOf`, `reviewGroup`, `evaluatePlatoon`,
   `reviewBench`, `roleOf`, ...) or, for a pipeline behavior, through `replacePacket` in `tests/mlbGolden.ts`.
4. Watch it fail for the reason you expect; fix the model; record the finding in the log in the hardening doc (BUG, SYSTEMATIC or
   DEBATABLE).
5. For a threshold, add the boundary to `mlbThresholdBoundaries.test.ts`. A hair either side, never "exactly on the line": an
   estimate is a blend of floating-point percentiles.

## What is deliberately not encoded

Who ranks first at a position, what any club's recommendation is, or any number that depends on the season's sample. Those change
with the game and would make the corpus a snapshot. The base-rate runs are the check on how often a flag fires, and they are
re-run when a threshold or a standard changes: `scripts/calibrate.ts standards` over all 30 clubs for MLB
Operations (counts in the hardening doc) and `npm run farm:base-rate` for the farm
(MINOR_LEAGUE_OPERATIONS.md §6.1).
