{
  "openapi": "3.1.0",
  "info": {
    "title": "Pennant",
    "version": "1",
    "description": "The presentation contract between the Pennant server and the Mac app (D-056). Generated by `npm run contract:build` from `server/contract/`; do not edit by hand."
  },
  "security": [
    {
      "bearer": []
    }
  ],
  "tags": [
    {
      "name": "v2",
      "description": "The Mac app's own API, under /api/v2."
    },
    {
      "name": "reused",
      "description": "Routes the React app already serves, described for the Mac app."
    }
  ],
  "paths": {
    "/api/config": {
      "post": {
        "operationId": "setSave",
        "summary": "Use this save's export folder, and import it. Refused (409) while an import runs.",
        "tags": [
          "reused"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConfigRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Ok"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be done",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiError"
                }
              }
            }
          },
          "409": {
            "description": "HTTP 409",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiError"
                }
              }
            }
          }
        }
      }
    },
    "/api/data-status": {
      "get": {
        "operationId": "getDataStatus",
        "summary": "How current the save, the export and the transaction log are, and where each was found.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DataStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/import": {
      "post": {
        "operationId": "startImport",
        "summary": "Import the configured save's export again; progress arrives on the event stream. Refused (409) while an import runs.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ImportAccepted"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be done",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiError"
                }
              }
            }
          },
          "409": {
            "description": "HTTP 409",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiError"
                }
              }
            }
          }
        }
      }
    },
    "/api/orgs": {
      "get": {
        "operationId": "listOrgs",
        "summary": "The major-league clubs, the one the save manages flagged, with their colours.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OrgList"
                }
              }
            }
          }
        }
      }
    },
    "/api/resolve-folder": {
      "post": {
        "operationId": "resolveFolder",
        "summary": "Check a folder the user picked: a CSV export, a save, or a folder of saves.",
        "tags": [
          "reused"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ResolveFolderRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResolveResult"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be done",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ResolveResult"
                }
              }
            }
          }
        }
      }
    },
    "/api/save-source": {
      "post": {
        "operationId": "setSaveSource",
        "summary": "Name the save folder by hand when it cannot be found from the export; empty returns to automatic.",
        "tags": [
          "reused"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SaveSourceRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveSourceResult"
                }
              }
            }
          },
          "400": {
            "description": "The request could not be done",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApiError"
                }
              }
            }
          }
        }
      }
    },
    "/api/saves": {
      "get": {
        "operationId": "listSaves",
        "summary": "The OOTP saves found in the usual places on this Mac.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SaveList"
                }
              }
            }
          }
        }
      }
    },
    "/api/search-locations": {
      "get": {
        "operationId": "getSearchLocations",
        "summary": "Where the server looked for saves, so an empty search can say why.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SearchLocations"
                }
              }
            }
          }
        }
      }
    },
    "/api/settings": {
      "get": {
        "operationId": "getSettings",
        "summary": "The preferences, the active provider's key state, the data folder and the current club.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SettingsResponse"
                }
              }
            }
          }
        }
      },
      "post": {
        "operationId": "saveSettings",
        "summary": "Change preferences (the club, the appearance, ...); a field left out keeps its value.",
        "tags": [
          "reused"
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SettingsUpdate"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SettingsSaved"
                }
              }
            }
          }
        }
      }
    },
    "/api/settings/providers": {
      "get": {
        "operationId": "getProviders",
        "summary": "The AI providers on offer and every provider's key state.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProvidersResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/status": {
      "get": {
        "operationId": "getStatus",
        "summary": "The save, the last import, a running import and a fresh export waiting.",
        "tags": [
          "reused"
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ServerStatus"
                }
              }
            }
          }
        }
      }
    },
    "/api/v2/events": {
      "get": {
        "operationId": "streamEvents",
        "summary": "Server-sent events: a hello with the status, then import, job and fresh-export news as it happens.",
        "tags": [
          "v2"
        ],
        "responses": {
          "200": {
            "description": "A stream of server-sent events; each event's data is a JSON ServerEvent.",
            "content": {
              "text/event-stream": {}
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearer": {
        "type": "http",
        "scheme": "bearer",
        "description": "The per-launch token the app hands the sidecar on stdin."
      }
    },
    "schemas": {
      "AiFeatureSettings": {
        "type": "object",
        "properties": {
          "provider": {
            "$ref": "#/components/schemas/ProviderId",
            "description": "Override the global provider for this feature."
          },
          "model": {
            "type": "string",
            "description": "Override the resolved model for this feature."
          }
        }
      },
      "ApiError": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string"
          }
        },
        "required": [
          "error"
        ],
        "description": "What a route answers when it cannot do what was asked (a 400)."
      },
      "ApiKeyStatus": {
        "type": "object",
        "properties": {
          "configured": {
            "type": "boolean"
          },
          "source": {
            "description": "`keychain`: handed over by the Mac app from the macOS Keychain.",
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "env",
                  "stored",
                  "keychain"
                ]
              },
              {
                "type": [
                  "string",
                  "null"
                ]
              }
            ]
          },
          "hint": {
            "type": [
              "string",
              "null"
            ]
          },
          "encrypted": {
            "type": "boolean"
          },
          "storageLabel": {
            "type": "string"
          }
        },
        "required": [
          "configured",
          "encrypted",
          "hint",
          "source",
          "storageLabel"
        ],
        "description": "The active provider's key state, with where keys are kept (`GET /api/settings`)."
      },
      "AppInfo": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "version": {
            "type": "string"
          },
          "projectUrl": {
            "type": "string"
          },
          "upstreamUrl": {
            "type": "string",
            "description": "The project this one began as a fork of; shown as credit, never used as a feed."
          }
        },
        "required": [
          "name",
          "version",
          "projectUrl",
          "upstreamUrl"
        ]
      },
      "Basis": {
        "type": "object",
        "properties": {
          "because": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/BasisLine"
            }
          },
          "source": {
            "$ref": "#/components/schemas/BasisSource"
          },
          "unknown": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "What is not known, as sentences. An empty list means nothing is missing, never \"not checked\"."
          },
          "wouldChange": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "What would change the reading, as sentences."
          },
          "lean": {
            "$ref": "#/components/schemas/Lean"
          },
          "certainty": {
            "$ref": "#/components/schemas/Certainty"
          },
          "stamp": {
            "type": "string",
            "description": "The D-041 stamp shown in the evidence view (a breakdown, where a method word may help)."
          }
        },
        "required": [
          "because",
          "source",
          "unknown",
          "wouldChange",
          "certainty"
        ],
        "description": "Why a claim says what it says (D-018, D-023, D-031, D-044: show the basis)."
      },
      "BasisLine": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          },
          "value": {
            "type": "string"
          }
        },
        "required": [
          "label",
          "value"
        ],
        "description": "One line of a claim's evidence: what was read and what it said."
      },
      "BasisSource": {
        "type": "object",
        "properties": {
          "department": {
            "$ref": "#/components/schemas/DeptId"
          },
          "specialist": {
            "type": "string",
            "description": "The specialist, in words (\"Data status\", \"Player Value\")."
          },
          "asOf": {
            "type": [
              "string",
              "null"
            ],
            "description": "When the evidence was read (an import's time, ISO 8601), or null when it has not been."
          },
          "gameDate": {
            "$ref": "#/components/schemas/GameDate",
            "description": "The game date the evidence reflects, as the export wrote it, or null."
          },
          "sample": {
            "type": "string",
            "description": "How much evidence there is, in words (\"30 games\"), when that matters."
          }
        },
        "required": [
          "department",
          "specialist",
          "asOf"
        ],
        "description": "Who answered: the department and its specialist, and how current the evidence is."
      },
      "Cell": {
        "type": "object",
        "properties": {
          "display": {
            "type": "string"
          },
          "tone": {
            "$ref": "#/components/schemas/Tone"
          },
          "hint": {
            "type": "string"
          },
          "claimRef": {
            "type": "string",
            "description": "A claim elsewhere in the payload that explains this cell."
          }
        },
        "required": [
          "display"
        ],
        "description": "One cell of a served table: what it shows, and its tone and help tag when it has them."
      },
      "Certainty": {
        "description": "How a claim's number or line is called (D-041): `calibrated` (fitted on the save's own history), `provisional` (a model parameter that ought to be fitted and has not been), `policy` (a line chosen and stated, never fitted), `unknown` (the basis cannot say), and `fact`: an objective fact read from the export or the save (a record, a date, how many days one source is behind another), which no fitted number and no chosen line decided. Section 4.1 listed the first four; `fact` was added at N4 so a plain fact is never stamped as policy.",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "fact",
              "calibrated",
              "provisional",
              "policy",
              "unknown"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "Claim": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The visible line."
          },
          "hint": {
            "type": "string",
            "description": "The help tag: one line, at most about 75 characters."
          },
          "value": {
            "$ref": "#/components/schemas/ServedValue"
          },
          "tone": {
            "$ref": "#/components/schemas/Tone"
          },
          "place": {
            "$ref": "#/components/schemas/Place"
          },
          "basis": {
            "$ref": "#/components/schemas/Basis"
          },
          "links": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Target"
            }
          }
        },
        "required": [
          "text",
          "tone",
          "basis",
          "links"
        ],
        "description": "One visible line, in the GM's voice, with its basis."
      },
      "ConfigRequest": {
        "type": "object",
        "properties": {
          "csvDir": {
            "type": "string"
          },
          "saveName": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "csvDir"
        ],
        "description": "The save to use (`POST /api/config`): its CSV export folder and its name."
      },
      "CurrentOrganization": {
        "type": "object",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/Integer"
          },
          "source": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "configured",
                  "human"
                ]
              },
              {
                "type": "string"
              }
            ]
          }
        },
        "required": [
          "id",
          "source"
        ],
        "description": "The club the app is about when nothing names one: the configured organization, else the one the save's human manages; null when neither is known. A configured id is taken as configured even when the club list no longer has it (the server's rule for every view), so a client shows that club as not found rather than quietly switching clubs."
      },
      "DataStatus": {
        "type": "object",
        "properties": {
          "generatedAt": {
            "type": "string"
          },
          "configured": {
            "type": "boolean"
          },
          "save": {
            "type": "object",
            "properties": {
              "found": {
                "type": "boolean"
              },
              "name": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "lgPath": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Diagnostic: where the `.lg` was found. Not something the user has to supply."
              },
              "discovery": {
                "$ref": "#/components/schemas/SaveDiscoveryMethod"
              },
              "discoveryNotes": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "simulatedThrough": {
                "$ref": "#/components/schemas/GameDate",
                "description": "Last simulated in-game date (ISO), from the save itself."
              },
              "dateSource": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "found",
              "name",
              "lgPath",
              "discovery",
              "discoveryNotes",
              "dateSource"
            ]
          },
          "csv": {
            "type": "object",
            "properties": {
              "currentDate": {
                "$ref": "#/components/schemas/GameDate",
                "description": "`leagues.current_date` of the imported export (ISO): the day about to be played."
              },
              "simulatedThrough": {
                "$ref": "#/components/schemas/GameDate",
                "description": "The last day the imported data reflects."
              },
              "exportedAt": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "importedAt": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "exportedAt",
              "importedAt"
            ]
          },
          "transactionLog": {
            "$ref": "#/components/schemas/LogSourceStatus"
          },
          "freshness": {
            "$ref": "#/components/schemas/FreshnessAssessment"
          }
        },
        "required": [
          "generatedAt",
          "configured",
          "save",
          "csv",
          "transactionLog",
          "freshness"
        ]
      },
      "DeptId": {
        "description": "A department of the club, as the Mac app's sidebar has them (SWIFTUI_REBUILD.md section 3.5). An open enum in the spec: a newer server may name a department an older app does not know.",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "frontOffice",
              "majorLeague",
              "farm",
              "scouting",
              "trades",
              "finance",
              "medical",
              "league",
              "philosophy"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "ExportPendingEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "export-pending"
            ]
          },
          "since": {
            "type": "string"
          }
        },
        "required": [
          "type",
          "since"
        ],
        "description": "OOTP has written a fresh export the server has not imported yet."
      },
      "FreshnessAssessment": {
        "type": "object",
        "properties": {
          "level": {
            "$ref": "#/components/schemas/RosterEvidenceLevel"
          },
          "save": {
            "type": "object",
            "properties": {
              "simulatedThrough": {
                "$ref": "#/components/schemas/GameDate"
              }
            }
          },
          "csv": {
            "type": "object",
            "properties": {
              "currentDate": {
                "$ref": "#/components/schemas/GameDate"
              },
              "state": {
                "$ref": "#/components/schemas/SourceState"
              },
              "through": {
                "$ref": "#/components/schemas/GameDate",
                "description": "Simulated-through date on the common basis, ISO."
              },
              "lagDays": {
                "$ref": "#/components/schemas/Integer"
              }
            },
            "required": [
              "lagDays",
              "state"
            ]
          },
          "log": {
            "type": "object",
            "properties": {
              "unavailableReason": {
                "$ref": "#/components/schemas/LogUnavailableReason"
              },
              "state": {
                "$ref": "#/components/schemas/SourceState"
              },
              "through": {
                "$ref": "#/components/schemas/GameDate",
                "description": "Simulated-through date on the common basis, ISO."
              },
              "lagDays": {
                "$ref": "#/components/schemas/Integer"
              }
            },
            "required": [
              "lagDays",
              "state"
            ]
          },
          "headline": {
            "type": "string",
            "description": "One line, e.g. \"Partial — transaction log 1 day behind\"."
          },
          "reasons": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "action": {
            "type": [
              "string",
              "null"
            ],
            "description": "What the user should do about it, when there is something to do."
          }
        },
        "required": [
          "level",
          "save",
          "csv",
          "log",
          "headline",
          "reasons",
          "action"
        ]
      },
      "GameDate": {
        "type": "string",
        "description": "An in-game date as the server serves it. A string, never a date type: OOTP writes dates unpadded (`2026-5-9`), and although the fields that pass through `parseGameDate` are padded, a client compares or orders game dates only as the server does (D-056). The contract (`contract/openapi.json`) names it `GameDate`, with no format."
      },
      "HelloEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "hello"
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ServerStatus"
          }
        },
        "required": [
          "type",
          "status"
        ],
        "description": "The first event on every stream: the `/api/status` snapshot, so nothing is missed between loading and listening."
      },
      "ImportAccepted": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean",
            "const": true
          },
          "lastImport": {
            "$ref": "#/components/schemas/ImportResult"
          },
          "lastError": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "ok",
          "lastError"
        ],
        "description": "What `POST /api/import` answers: the import has started; its progress and result arrive on `/api/status` and the event stream."
      },
      "ImportFinishedEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "import-finished"
            ]
          },
          "lastImport": {
            "$ref": "#/components/schemas/ImportResult"
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "type",
          "error"
        ]
      },
      "ImportProgress": {
        "type": "object",
        "properties": {
          "table": {
            "type": "string",
            "description": "The table being written, as OOTP names the file."
          },
          "fileIndex": {
            "$ref": "#/components/schemas/Integer",
            "description": "1-based, so it reads as \"12 of 70\" without arithmetic."
          },
          "files": {
            "$ref": "#/components/schemas/Integer"
          },
          "rows": {
            "$ref": "#/components/schemas/Integer",
            "description": "Rows written so far, across every table."
          },
          "phase": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "reading",
                  "writing",
                  "indexing"
                ]
              },
              {
                "type": "string"
              }
            ]
          }
        },
        "required": [
          "table",
          "fileIndex",
          "files",
          "rows",
          "phase"
        ],
        "description": "Where the import has got to, for a page that would rather not look frozen."
      },
      "ImportProgressEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "import-progress"
            ]
          },
          "progress": {
            "$ref": "#/components/schemas/ImportProgress"
          }
        },
        "required": [
          "type",
          "progress"
        ]
      },
      "ImportResult": {
        "type": "object",
        "properties": {
          "tables": {
            "$ref": "#/components/schemas/Integer"
          },
          "rows": {
            "$ref": "#/components/schemas/Integer"
          },
          "startedAt": {
            "type": "string"
          },
          "finishedAt": {
            "type": "string"
          },
          "files": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "table": {
                  "type": "string"
                },
                "rows": {
                  "$ref": "#/components/schemas/Integer"
                }
              },
              "required": [
                "table",
                "rows"
              ]
            }
          }
        },
        "required": [
          "tables",
          "rows",
          "startedAt",
          "finishedAt",
          "files"
        ]
      },
      "ImportStartedEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "import-started"
            ]
          },
          "startedAt": {
            "type": "string"
          }
        },
        "required": [
          "type",
          "startedAt"
        ]
      },
      "Integer": {
        "type": "integer",
        "description": "A whole number: an id, a count, a season, a number of days. The spec says `type: integer`, so the Mac app reads an `Int`, not a `Double`. A true decimal (a rate, a slider, dollars) stays `number`. Named `Integer`, never `Int`, which the Swift generator would rename."
      },
      "JobEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "job"
            ]
          },
          "kind": {
            "type": "string"
          },
          "orgId": {
            "$ref": "#/components/schemas/Integer"
          },
          "status": {
            "$ref": "#/components/schemas/JobStatus"
          }
        },
        "required": [
          "type",
          "kind",
          "orgId",
          "status"
        ],
        "description": "A background job (storylines, the briefing) changed state for one club."
      },
      "JobState": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "idle",
              "running",
              "done",
              "error"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "JobStatus": {
        "type": "object",
        "properties": {
          "state": {
            "$ref": "#/components/schemas/JobState"
          },
          "startedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "finishedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "error": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "state",
          "startedAt",
          "finishedAt",
          "error"
        ]
      },
      "KeyStatus": {
        "type": "object",
        "properties": {
          "configured": {
            "type": "boolean"
          },
          "source": {
            "description": "`keychain`: handed over by the Mac app from the macOS Keychain.",
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "env",
                  "stored",
                  "keychain"
                ]
              },
              {
                "type": [
                  "string",
                  "null"
                ]
              }
            ]
          },
          "hint": {
            "type": [
              "string",
              "null"
            ]
          },
          "encrypted": {
            "type": "boolean"
          }
        },
        "required": [
          "configured",
          "source",
          "hint",
          "encrypted"
        ]
      },
      "Lean": {
        "type": "object",
        "properties": {
          "neutral": {
            "type": "string"
          },
          "why": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "neutral",
          "why"
        ],
        "description": "The organization's philosophy leaning on a reading (D-052's lens): the neutral reading beside the reasons for the lean. A claim with no lean says so with `null`; an absent lean is never read as \"neutral\"."
      },
      "LiveLogFailure": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "not_found",
              "torn",
              "invalid",
              "copy_failed"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "LogCoverage": {
        "type": "object",
        "properties": {
          "firstTransactionDate": {
            "$ref": "#/components/schemas/GameDate"
          },
          "lastTransactionDate": {
            "$ref": "#/components/schemas/GameDate",
            "description": "The newest date on any transaction row."
          },
          "activityThrough": {
            "$ref": "#/components/schemas/GameDate",
            "description": "The newest date on any day-stamped table in the database."
          },
          "coveredThrough": {
            "$ref": "#/components/schemas/GameDate",
            "description": "How far the database has demonstrably been written. Days with no transactions (off-season, an off-day) leave `lastTransactionDate` behind without the log being behind, so this takes the newest of everything."
          },
          "season": {
            "$ref": "#/components/schemas/Integer"
          }
        }
      },
      "LogSourceStatus": {
        "type": "object",
        "properties": {
          "found": {
            "type": "boolean",
            "description": "The live transaction database exists in the save's temp folder."
          },
          "readable": {
            "type": "boolean"
          },
          "error": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "code": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/LiveLogFailure"
                  },
                  {
                    "type": "string",
                    "enum": [
                      "missing"
                    ]
                  }
                ]
              },
              "message": {
                "type": "string"
              }
            },
            "required": [
              "code",
              "message"
            ]
          },
          "unavailableReason": {
            "$ref": "#/components/schemas/LogUnavailableReason"
          },
          "files": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "db": {
                "type": "boolean"
              },
              "wal": {
                "type": "boolean"
              },
              "shm": {
                "type": "boolean"
              }
            },
            "required": [
              "db",
              "wal",
              "shm"
            ]
          },
          "snapshot": {
            "$ref": "#/components/schemas/SnapshotMeta"
          },
          "coverage": {
            "$ref": "#/components/schemas/LogCoverage"
          },
          "counts": {
            "type": [
              "object",
              "null"
            ],
            "properties": {
              "events": {
                "$ref": "#/components/schemas/Integer"
              },
              "unsupported": {
                "$ref": "#/components/schemas/Integer"
              },
              "byKind": {
                "type": "object",
                "properties": {
                  "optioned": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "recalled": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "purchased_contract": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "designated_for_assignment": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "il_placed": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "il_activated": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "restricted_list_placed": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "restricted_list_activated": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "released": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "rule5_return": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "rehab_assigned": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "rehab_received": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "rehab_returned": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "minor_league_assignment": {
                    "$ref": "#/components/schemas/Integer"
                  },
                  "unsupported": {
                    "$ref": "#/components/schemas/Integer"
                  }
                }
              }
            },
            "required": [
              "events",
              "unsupported",
              "byKind"
            ]
          },
          "unsupportedSamples": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "found",
          "readable",
          "error",
          "files",
          "counts",
          "unsupportedSamples"
        ]
      },
      "LogUnavailableReason": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "save_not_found",
              "database_missing",
              "unreadable"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "Ok": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean",
            "const": true
          }
        },
        "required": [
          "ok"
        ],
        "description": "A request the server accepted, with nothing more to say."
      },
      "Org": {
        "type": "object",
        "properties": {
          "team_id": {
            "$ref": "#/components/schemas/Integer"
          },
          "label": {
            "type": "string"
          },
          "isHuman": {
            "type": "boolean"
          },
          "colors": {
            "type": "object",
            "properties": {
              "bg": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "fg": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "secondary": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "cap": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "bg",
              "fg",
              "secondary",
              "cap"
            ]
          }
        },
        "required": [
          "team_id",
          "label",
          "isHuman",
          "colors"
        ],
        "description": "A major-league club as `GET /api/orgs` lists it."
      },
      "OrgList": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/Org"
        },
        "description": "The major-league clubs (`GET /api/orgs`)."
      },
      "PhilosophyDimensionId": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "competitiveWindow",
              "riskTolerance",
              "payrollFlexibility",
              "costEfficiency",
              "teamControl",
              "prospectPreservation",
              "promotionAggressiveness",
              "upsidePreference",
              "ageCurveSensitivity",
              "positionalScarcity",
              "defenseEmphasis",
              "pitchingDepth",
              "rosterDepth",
              "starConcentration",
              "versatility"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "PhilosophyMode": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "manual",
              "staff",
              "hybrid"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "PhilosophyPolicies": {
        "type": "object",
        "properties": {
          "agingContracts": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "avoid",
                  "discourage",
                  "neutral",
                  "willing"
                ]
              },
              {
                "type": "string"
              }
            ]
          },
          "arbitrationExtensions": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "avoid",
                  "selective",
                  "prefer"
                ]
              },
              {
                "type": "string"
              }
            ]
          },
          "rentalAcquisitions": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "never",
                  "contending",
                  "normal",
                  "aggressive"
                ]
              },
              {
                "type": "string"
              }
            ]
          },
          "salaryDumps": {
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "avoid",
                  "neutral",
                  "willing"
                ]
              },
              {
                "type": "string"
              }
            ]
          }
        },
        "required": [
          "agingContracts",
          "arbitrationExtensions",
          "rentalAcquisitions",
          "salaryDumps"
        ]
      },
      "PhilosophyProfile": {
        "type": "object",
        "properties": {
          "version": {
            "type": "number",
            "const": 1
          },
          "mode": {
            "$ref": "#/components/schemas/PhilosophyMode",
            "description": "V1 uses manual. Staff and hybrid exist now so V2 can be introduced without changing the persisted shape."
          },
          "manual": {
            "$ref": "#/components/schemas/PhilosophyValues",
            "description": "User-selected values. Always available as a fallback."
          },
          "overrides": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PhilosophyDimensionId"
            },
            "description": "In hybrid mode these dimensions stay user-controlled while the others may come from staff."
          },
          "policies": {
            "$ref": "#/components/schemas/PhilosophyPolicies"
          }
        },
        "required": [
          "version",
          "mode",
          "manual",
          "overrides",
          "policies"
        ]
      },
      "PhilosophyValues": {
        "type": "object",
        "properties": {
          "competitiveWindow": {
            "type": "number"
          },
          "riskTolerance": {
            "type": "number"
          },
          "payrollFlexibility": {
            "type": "number"
          },
          "costEfficiency": {
            "type": "number"
          },
          "teamControl": {
            "type": "number"
          },
          "prospectPreservation": {
            "type": "number"
          },
          "promotionAggressiveness": {
            "type": "number"
          },
          "upsidePreference": {
            "type": "number"
          },
          "ageCurveSensitivity": {
            "type": "number"
          },
          "positionalScarcity": {
            "type": "number"
          },
          "defenseEmphasis": {
            "type": "number"
          },
          "pitchingDepth": {
            "type": "number"
          },
          "rosterDepth": {
            "type": "number"
          },
          "starConcentration": {
            "type": "number"
          },
          "versatility": {
            "type": "number"
          }
        },
        "required": [
          "competitiveWindow",
          "riskTolerance",
          "payrollFlexibility",
          "costEfficiency",
          "teamControl",
          "prospectPreservation",
          "promotionAggressiveness",
          "upsidePreference",
          "ageCurveSensitivity",
          "positionalScarcity",
          "defenseEmphasis",
          "pitchingDepth",
          "rosterDepth",
          "starConcentration",
          "versatility"
        ]
      },
      "Place": {
        "type": "object",
        "properties": {
          "rank": {
            "$ref": "#/components/schemas/Integer"
          },
          "of": {
            "$ref": "#/components/schemas/Integer"
          },
          "tiedWith": {
            "$ref": "#/components/schemas/Integer"
          },
          "overlap": {
            "$ref": "#/components/schemas/Integer"
          }
        },
        "required": [
          "rank",
          "of",
          "tiedWith"
        ],
        "description": "A league place, stated (D-057): `rank` among `of` clubs that have the figure, `tiedWith` the number of other clubs on the same figure, and, for a Player Value place, `overlap`: how many clubs' ranges overlap this one."
      },
      "ProviderChoice": {
        "type": "object",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/ProviderId"
          },
          "label": {
            "type": "string"
          },
          "keyLabel": {
            "type": "string"
          },
          "console": {
            "type": "string"
          },
          "requiresKey": {
            "type": "boolean"
          },
          "model": {
            "type": "string"
          }
        },
        "required": [
          "console",
          "id",
          "keyLabel",
          "label",
          "model",
          "requiresKey"
        ],
        "description": "A provider on offer, with the model it would use (`GET /api/settings/providers`)."
      },
      "ProviderId": {
        "description": "Four ways to reach a model, behind one shape.\n\nThe app was written against Anthropic and every feature still works best there — the chat's tool loop and prompt caching are Anthropic's shapes, and this is where they stay. What this adds is a choice for anyone who already pays for a key somewhere else, which is most of the reason people asked.\n\nOnly the parts that genuinely differ are adapted. A system prompt is a separate argument at Anthropic, the first message at OpenAI, and its own field at Google; a refusal is a stop reason, a field on the message, and a finish reason respectively. Those are normalised here so the four call sites upstream never learn which provider answered.",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "anthropic",
              "openai",
              "gemini",
              "opencode",
              "ollama"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "ProviderInfo": {
        "type": "object",
        "properties": {
          "id": {
            "$ref": "#/components/schemas/ProviderId"
          },
          "label": {
            "type": "string"
          },
          "keyLabel": {
            "type": "string"
          },
          "console": {
            "type": "string"
          },
          "requiresKey": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "label",
          "keyLabel",
          "console",
          "requiresKey"
        ],
        "description": "One AI service the app can talk to, as the Settings screen lists it."
      },
      "ProvidersResponse": {
        "type": "object",
        "properties": {
          "providers": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderChoice"
            }
          },
          "keys": {
            "type": "object",
            "properties": {
              "anthropic": {
                "$ref": "#/components/schemas/KeyStatus"
              },
              "openai": {
                "$ref": "#/components/schemas/KeyStatus"
              },
              "gemini": {
                "$ref": "#/components/schemas/KeyStatus"
              },
              "opencode": {
                "$ref": "#/components/schemas/KeyStatus"
              },
              "ollama": {
                "$ref": "#/components/schemas/KeyStatus"
              }
            },
            "required": [
              "anthropic",
              "openai",
              "gemini",
              "opencode",
              "ollama"
            ]
          }
        },
        "required": [
          "providers",
          "keys"
        ],
        "description": "What `GET /api/settings/providers` serves: the providers on offer and every provider's key state."
      },
      "ResolveFolderRequest": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "description": "The folder the user picked or typed (`POST /api/resolve-folder`)."
      },
      "ResolveResult": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean"
          },
          "csvDir": {
            "type": "string"
          },
          "saveName": {
            "type": "string"
          },
          "csvCount": {
            "$ref": "#/components/schemas/Integer"
          },
          "saves": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SaveInfo"
            },
            "description": "Saves found inside the chosen folder, when it holds several."
          },
          "error": {
            "type": "string"
          }
        },
        "required": [
          "ok"
        ]
      },
      "RosterEvidenceLevel": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "current",
              "partial",
              "stale",
              "unavailable"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "SaveDiscoveryMethod": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "csv_layout",
              "ancestor_lg",
              "save_name_match",
              "manual_override",
              "not_found"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "SaveInfo": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "lgPath": {
            "type": "string"
          },
          "csvDir": {
            "type": "string"
          },
          "csvCount": {
            "$ref": "#/components/schemas/Integer"
          },
          "csvLastModified": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "name",
          "lgPath",
          "csvDir",
          "csvCount",
          "csvLastModified"
        ]
      },
      "SaveList": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/SaveInfo"
        },
        "description": "The saves found in the usual places (`GET /api/saves`)."
      },
      "SaveSourceRequest": {
        "type": "object",
        "properties": {
          "lgPath": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "description": "The `<save>.lg` folder named by hand (`POST /api/save-source`); empty or null returns to automatic."
      },
      "SaveSourceResult": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean",
            "const": true
          },
          "status": {
            "$ref": "#/components/schemas/DataStatus"
          }
        },
        "required": [
          "ok",
          "status"
        ],
        "description": "What `POST /api/save-source` answers once the save folder is set or cleared."
      },
      "SearchLocation": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string"
          },
          "path": {
            "type": "string"
          },
          "exists": {
            "type": "boolean"
          }
        },
        "required": [
          "label",
          "path",
          "exists"
        ],
        "description": "One place the server looks for saves: a human label, the folder, and whether it exists here."
      },
      "SearchLocations": {
        "type": "object",
        "properties": {
          "platform": {
            "type": "string"
          },
          "locations": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SearchLocation"
            }
          }
        },
        "required": [
          "platform",
          "locations"
        ],
        "description": "Where `GET /api/search-locations` looked for saves, so the user can see why auto-detection came up empty."
      },
      "ServedValue": {
        "type": "object",
        "properties": {
          "n": {
            "type": [
              "number",
              "null"
            ]
          },
          "unit": {
            "$ref": "#/components/schemas/Unit"
          },
          "low": {
            "type": "number",
            "description": "The low end of the range, when the value is a range (the most likely value is `n`)."
          },
          "high": {
            "type": "number"
          },
          "display": {
            "type": "string",
            "description": "The value as the app shows it (\"4.8 runs a game\", \"Not known yet\")."
          }
        },
        "required": [
          "n",
          "unit",
          "display"
        ],
        "description": "A number a claim shows, with its range when it has one. `n` null is a value the evidence cannot give; `display` always says something (the sentence for a missing value, never a zero or a dash)."
      },
      "ServerEvent": {
        "anyOf": [
          {
            "$ref": "#/components/schemas/HelloEvent"
          },
          {
            "$ref": "#/components/schemas/ImportStartedEvent"
          },
          {
            "$ref": "#/components/schemas/ImportProgressEvent"
          },
          {
            "$ref": "#/components/schemas/ImportFinishedEvent"
          },
          {
            "$ref": "#/components/schemas/ExportPendingEvent"
          },
          {
            "$ref": "#/components/schemas/JobEvent"
          },
          {
            "$ref": "#/components/schemas/UnknownServerEvent"
          }
        ],
        "description": "What the server tells a connected app as it happens, over `GET /api/v2/events` (server-sent events, D-055).\n\nThe React app polls `/api/status` every few seconds; the Mac app listens here instead. Every event is a fact the server already holds (the same fields `/api/status` and the job routes serve), pushed when it changes. An event is a nudge to re-read, never a second source of truth: a client that missed one reads `/api/status`, and the stream opens with a `hello` carrying that snapshot so nothing is missed between loading and listening.\n\nThe names are the contract's (N2 describes them in `contract/openapi.json`); a client ignores a name it does not know, so new events can be added without breaking an older app."
      },
      "ServerStatus": {
        "type": "object",
        "properties": {
          "app": {
            "$ref": "#/components/schemas/AppInfo",
            "description": "Product name and version, from package.json (see appInfo.ts)."
          },
          "csvExportedAt": {
            "type": [
              "string",
              "null"
            ]
          },
          "configured": {
            "type": "boolean"
          },
          "saveName": {
            "type": [
              "string",
              "null"
            ]
          },
          "csvDir": {
            "type": [
              "string",
              "null"
            ]
          },
          "csvDirExists": {
            "type": "boolean"
          },
          "importing": {
            "type": "boolean"
          },
          "importProgress": {
            "$ref": "#/components/schemas/ImportProgress",
            "description": "Where a running import has got to; null when nothing is importing."
          },
          "lastImport": {
            "$ref": "#/components/schemas/ImportResult"
          },
          "lastError": {
            "type": [
              "string",
              "null"
            ]
          },
          "importInterruptedSince": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when an import stopped partway (the server was stopped or crashed); cleared by the next completed import."
          },
          "hasData": {
            "type": "boolean"
          },
          "exportPending": {
            "type": [
              "string",
              "null"
            ],
            "description": "Set when OOTP has written a fresh export the app has not imported yet."
          },
          "logoToken": {
            "type": "string",
            "description": "Changes with the save, and rides along on every logo URL."
          },
          "ratingScaleMax": {
            "$ref": "#/components/schemas/Integer",
            "description": "The top of the rating scale the save shows ratings on."
          }
        },
        "required": [
          "app",
          "csvExportedAt",
          "configured",
          "saveName",
          "csvDir",
          "csvDirExists",
          "importing",
          "lastError",
          "importInterruptedSince",
          "hasData",
          "exportPending",
          "logoToken",
          "ratingScaleMax"
        ],
        "description": "What `/api/status` serves, and the snapshot `/api/v2/events` opens with (described in `contract/openapi.json`)."
      },
      "Settings": {
        "type": "object",
        "properties": {
          "autoImport": {
            "type": "boolean"
          },
          "useTeamColors": {
            "type": "boolean"
          },
          "defaultOrgId": {
            "$ref": "#/components/schemas/Integer"
          },
          "theme": {
            "description": "'system' follows the OS setting and changes with it.",
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "system",
                  "dark",
                  "light"
                ]
              },
              {
                "type": "string"
              }
            ]
          },
          "model": {
            "type": "string",
            "description": "Model id used by every AI feature. See models.ts for the picker's list."
          },
          "provider": {
            "$ref": "#/components/schemas/ProviderId",
            "description": "Which service the AI features talk to. Anthropic unless changed — it is what the app was built against and the only one the chat's tool loop and prompt caching are native to."
          },
          "models": {
            "type": "object",
            "properties": {
              "anthropic": {
                "type": "string"
              },
              "openai": {
                "type": "string"
              },
              "gemini": {
                "type": "string"
              },
              "opencode": {
                "type": "string"
              },
              "ollama": {
                "type": "string"
              }
            },
            "description": "The model chosen for each provider, remembered separately. Switching provider would otherwise leave a model id the new one has never heard of."
          },
          "aiFeatures": {
            "type": "object",
            "properties": {
              "briefing": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "trade": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "storylines": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "chat": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              }
            },
            "description": "Optional provider/model overrides for individual AI workloads.\n\nMissing entries inherit the global provider and that provider's selected model, preserving the behaviour of existing settings files."
          },
          "roundRatingsToFive": {
            "type": "boolean",
            "description": "Write overall and potential in fives, the way scouts talk. Display only — sorting and every calculation keep the exact grade."
          },
          "showUnavailablePitchers": {
            "type": "boolean",
            "description": "Keep pitchers on the injured list in the bullpen table.\n\nOff unless asked for. That page answers one question — who can throw tonight — and a man six weeks from a rehab start is not an answer to it. Nothing vanishes quietly: the heading says how many are being held back and hands them over in a click."
          },
          "autoGenerateAfterImport": {
            "type": "boolean",
            "description": "Write the storylines and the briefing by themselves after each import.\n\nOff unless asked for. Both cost money on someone else's API key, and a setting that quietly spends it would be a poor default however convenient."
          },
          "nextSeasonBudget": {
            "type": "object",
            "additionalProperties": {
              "type": "number"
            },
            "description": "What you expect the owner to hand you next season, per club, in dollars.\n\nOOTP does not publish a future budget — it is not set until the offseason — so future headroom had to assume this year's holds flat. On a club whose budget swings, that is the wrong number to plan against, and you are the one who knows which way the owner leans. Absent an entry, flat it stays."
          },
          "organizationPhilosophies": {
            "type": "object",
            "additionalProperties": {
              "$ref": "#/components/schemas/PhilosophyProfile"
            },
            "description": "Organizational philosophy is saved per club. The string key is the OOTP organization/team id so different clubs and saves may think differently."
          }
        },
        "required": [
          "autoImport",
          "useTeamColors",
          "theme",
          "model",
          "provider",
          "models",
          "aiFeatures",
          "roundRatingsToFive",
          "showUnavailablePitchers",
          "autoGenerateAfterImport",
          "nextSeasonBudget",
          "organizationPhilosophies"
        ],
        "description": "User preferences, plus the Anthropic API key.\n\nThe key is a real secret, so it is never written in plain text when we can avoid it: the desktop app hands us Electron's safeStorage, which encrypts against the OS keychain (Keychain on macOS, DPAPI on Windows). Running from source in a browser there is no keychain available, so the key falls back to a 0600 file and the UI says so plainly rather than implying it is protected."
      },
      "SettingsResponse": {
        "type": "object",
        "properties": {
          "settings": {
            "$ref": "#/components/schemas/Settings"
          },
          "apiKey": {
            "$ref": "#/components/schemas/ApiKeyStatus"
          },
          "dataDir": {
            "type": "string"
          },
          "organization": {
            "$ref": "#/components/schemas/CurrentOrganization",
            "description": "The club the app is about: the configured organization, else the human-managed one (`viewingOrganization.ts`)."
          }
        },
        "required": [
          "settings",
          "apiKey",
          "dataDir"
        ],
        "description": "What `GET /api/settings` serves."
      },
      "SettingsSaved": {
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean",
            "const": true
          },
          "settings": {
            "$ref": "#/components/schemas/Settings"
          }
        },
        "required": [
          "ok",
          "settings"
        ],
        "description": "What `POST /api/settings` answers: the preferences as saved."
      },
      "SettingsUpdate": {
        "type": "object",
        "properties": {
          "autoImport": {
            "type": "boolean"
          },
          "useTeamColors": {
            "type": "boolean"
          },
          "roundRatingsToFive": {
            "type": "boolean",
            "description": "Write overall and potential in fives, the way scouts talk. Display only — sorting and every calculation keep the exact grade."
          },
          "showUnavailablePitchers": {
            "type": "boolean",
            "description": "Keep pitchers on the injured list in the bullpen table.\n\nOff unless asked for. That page answers one question — who can throw tonight — and a man six weeks from a rehab start is not an answer to it. Nothing vanishes quietly: the heading says how many are being held back and hands them over in a click."
          },
          "autoGenerateAfterImport": {
            "type": "boolean",
            "description": "Write the storylines and the briefing by themselves after each import.\n\nOff unless asked for. Both cost money on someone else's API key, and a setting that quietly spends it would be a poor default however convenient."
          },
          "defaultOrgId": {
            "$ref": "#/components/schemas/Integer"
          },
          "theme": {
            "description": "'system' follows the OS setting and changes with it.",
            "anyOf": [
              {
                "type": "string",
                "enum": [
                  "system",
                  "dark",
                  "light"
                ]
              },
              {
                "type": "string"
              }
            ]
          },
          "provider": {
            "$ref": "#/components/schemas/ProviderId",
            "description": "Which service the AI features talk to. Anthropic unless changed — it is what the app was built against and the only one the chat's tool loop and prompt caching are native to."
          },
          "model": {
            "type": "string",
            "description": "Model id used by every AI feature. See models.ts for the picker's list."
          },
          "models": {
            "type": "object",
            "properties": {
              "anthropic": {
                "type": "string"
              },
              "openai": {
                "type": "string"
              },
              "gemini": {
                "type": "string"
              },
              "opencode": {
                "type": "string"
              },
              "ollama": {
                "type": "string"
              }
            },
            "description": "The model chosen for each provider, remembered separately. Switching provider would otherwise leave a model id the new one has never heard of."
          },
          "aiFeatures": {
            "type": "object",
            "properties": {
              "briefing": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "trade": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "storylines": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              },
              "chat": {
                "$ref": "#/components/schemas/AiFeatureSettings"
              }
            },
            "description": "Optional provider/model overrides for individual AI workloads.\n\nMissing entries inherit the global provider and that provider's selected model, preserving the behaviour of existing settings files."
          }
        },
        "description": "What `POST /api/settings` takes: the preferences to change. A field left out keeps its value, and a value the handler cannot use is ignored. (Philosophy and next season's budget have routes of their own.)"
      },
      "SnapshotMeta": {
        "type": "object",
        "properties": {
          "attempts": {
            "$ref": "#/components/schemas/Integer"
          },
          "walCopied": {
            "type": "boolean"
          },
          "shmPresent": {
            "type": "boolean",
            "description": "Whether the source had a `-shm` file. It is never copied."
          },
          "dbBytes": {
            "$ref": "#/components/schemas/Integer"
          },
          "walBytes": {
            "$ref": "#/components/schemas/Integer"
          },
          "sourceModified": {
            "type": "object",
            "properties": {
              "db": {
                "type": [
                  "string",
                  "null"
                ]
              },
              "wal": {
                "type": [
                  "string",
                  "null"
                ]
              }
            },
            "required": [
              "db",
              "wal"
            ],
            "description": "Modification times of the source files when copied."
          },
          "copiedAt": {
            "type": "string"
          }
        },
        "required": [
          "attempts",
          "walCopied",
          "shmPresent",
          "dbBytes",
          "walBytes",
          "sourceModified",
          "copiedAt"
        ]
      },
      "SourceState": {
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "current",
              "behind",
              "unverified",
              "unavailable"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "Target": {
        "type": "object",
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/TargetKind"
          },
          "department": {
            "$ref": "#/components/schemas/DeptId"
          },
          "view": {
            "type": "string",
            "description": "A view's id inside its department (`fortyManOptions`)."
          },
          "playerId": {
            "$ref": "#/components/schemas/Integer"
          },
          "teamId": {
            "$ref": "#/components/schemas/Integer"
          },
          "key": {
            "type": "string",
            "description": "A decision's key inside its department."
          }
        },
        "required": [
          "kind"
        ],
        "description": "Where a claim or a row leads: a department, one of its views, a player, a club or a decision. Flat, with the fields its kind needs, so the app reads one shape."
      },
      "TargetKind": {
        "description": "What a target opens.",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "department",
              "view",
              "player",
              "club",
              "decision"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "Tone": {
        "description": "How a line reads at a glance. Colour is never the only signal: the app pairs a tone with a symbol and a word.",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "good",
              "bad",
              "caution",
              "neutral",
              "unknown"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "Unit": {
        "description": "What a served number counts, so the app formats it (a share as a percentage, dollars as money).",
        "anyOf": [
          {
            "type": "string",
            "enum": [
              "count",
              "share",
              "rate",
              "dollars",
              "wins",
              "runs",
              "games",
              "days",
              "years",
              "place"
            ]
          },
          {
            "type": "string"
          }
        ]
      },
      "UnknownServerEvent": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string"
          }
        },
        "required": [
          "type"
        ],
        "description": "An event this build of the contract does not name, sent by a newer server. The spec lists it last among `ServerEvent`'s shapes, so an older app decodes it (and ignores it) instead of failing the stream."
      }
    }
  }
}
